---
phase: 03-eval-engine
plan: 01
type: tdd
wave: 0
depends_on: []
files_modified:
  - scripts/test-eval.ts
autonomous: true
requirements:
  - EVAL-01
  - EVAL-03
  - EVAL-04
  - EVAL-05
  - EVAL-06
  - EVAL-07

must_haves:
  truths:
    - "Running `npx tsx scripts/test-eval.ts` exits 0 with all assertions passing"
    - "Schema validation failure path is tested: composite 0, schema_valid false, validation_error set"
    - "Composite formula is verified against known inputs: all-5 → 100, all-1 → 20, mixed → correct"
    - "JudgeResponse Zod schema parsing is verified: valid judge JSON passes, malformed fails"
    - "writeScored creates results/<run-id>/scored/<model-slug>.json with correct shape"
    - "upsertIndex adds a new entry and replaces an existing entry for the same run_id"
  artifacts:
    - path: "scripts/test-eval.ts"
      provides: "All automated tests for the eval engine"
      exports: []
  key_links:
    - from: "scripts/test-eval.ts"
      to: "src/eval.ts"
      via: "import { computeComposite, JudgeResponse, writeScored, upsertIndex } from"
      pattern: "from.*src/eval"
---

<objective>
Create the Wave 0 test scaffold that defines the behavioral contract for the eval engine.

Purpose: Nyquist rule — tests must exist (and fail RED) before implementation. This plan locks the expected behavior for all six EVAL requirements so Plan 02 can implement to a clear spec.
Output: scripts/test-eval.ts with unit and integration tests covering EVAL-01 through EVAL-07. Tests will fail until Plan 02 creates src/eval.ts.
</objective>

<execution_context>
@/Users/obinnaeto/.claude/get-shit-done/workflows/execute-plan.md
@/Users/obinnaeto/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-eval-engine/03-CONTEXT.md
@.planning/phases/03-eval-engine/03-RESEARCH.md
@.planning/phases/03-eval-engine/03-VALIDATION.md
</context>

<interfaces>
<!-- Contracts the test file will import against. src/eval.ts does not exist yet — tests define what it must export. -->

Expected exports from src/eval.ts (to be created in Plan 02):
```typescript
// Zod schema validating the judge's JSON response
export const JudgeResponse: z.ZodObject<...>;
export type JudgeResponseType = z.infer<typeof JudgeResponse>;

// Composite score formula: (a + rt + c) / 3 * 20, rounded to integer, range 20-100
export function computeComposite(scores: JudgeResponseType): number;

// Write scored result to results/<run-id>/scored/<model-slug>.json
export async function writeScored(opts: {
  runDir: string;
  modelSlug: string;
  scoredResult: ScoredResult;
}): Promise<void>;

// Upsert index.json entry (deduplicated by run_id)
export async function upsertIndex(opts: {
  indexPath: string;
  entry: IndexEntry;
}): Promise<void>;

// Main eval orchestrator (called from cli.ts)
export async function runEval(opts: {
  runDir: string;
  runId: string;
  harnessName: string;
  rawOutput: unknown;
  modelSlug: string;
  spec: HarnessSpec;
  meta: { cost_usd: number; latency_ms: number; run_date: string };
}): Promise<void>;
```

From src/types.ts (existing):
```typescript
export interface HarnessSpec {
  name: string;
  version: string;
  eval: { rubric: string; judge_model: string; judge_temperature: number };
}
```

From scripts/test-output.ts (existing pattern — Node assert, no framework):
```typescript
import assert from "node:assert/strict";
import { rm } from "fs/promises";
// Use tmp directories under results/ for integration tests, clean up in finally
```
</interfaces>

<tasks>

<task type="tdd">
  <name>Task 1: Write test-eval.ts covering all EVAL requirements</name>
  <files>scripts/test-eval.ts</files>
  <behavior>
    Unit — computeComposite:
    - computeComposite({ actionability: {score:5}, reasoning_transparency: {score:5}, completeness: {score:5} }) === 100
    - computeComposite({ actionability: {score:1}, reasoning_transparency: {score:1}, completeness: {score:1} }) === 20
    - computeComposite({ actionability: {score:4}, reasoning_transparency: {score:3}, completeness: {score:5} }) === 80  (4+3+5=12, 12/3=4, 4*20=80)
    - computeComposite({ actionability: {score:3}, reasoning_transparency: {score:3}, completeness: {score:4} }) === Math.round((10/3)*20)  (verify rounding is correct)

    Unit — JudgeResponse Zod schema:
    - Valid judge JSON parses successfully: { actionability: {score:4,rationale:"good"}, reasoning_transparency: {score:3,rationale:"ok"}, completeness: {score:5,rationale:"thorough"} }
    - Invalid judge JSON fails parse: score out of range (score:6), missing rationale, missing dimension key
    - Score 0 fails parse (min is 1 — zero is only for schema validation failures, not a valid judge score)

    Integration — writeScored:
    - Creates results/<tmp-run-id>/scored/<slug>.json
    - File contents are valid JSON with expected shape (schema_valid, composite_score, scores object with 3 dimensions)

    Integration — upsertIndex:
    - Appends new entry to non-existent index.json (creates file)
    - Appends new entry to existing index.json with different run_id (file has 2 entries)
    - Replaces existing entry when same run_id is re-scored (file still has 1 entry, updated values)

    Unit — schema validation failure path:
    - When rawOutput fails Zod safeParse: scores are all 0, composite_score is 0, schema_valid is false, validation_error is a non-empty string
    - Judge is NOT called when schema validation fails (this is structural — test that the returned result has schema_valid: false without needing to mock the judge)
  </behavior>
  <action>
    Create scripts/test-eval.ts using Node built-in `assert` (no test framework — matches Phase 2 pattern from test-output.ts, test-routing.ts etc.).

    Structure:
    1. Import assert from "node:assert/strict", mkdir/rm from "fs/promises", join from "path"
    2. Import the expected exports from "../src/eval.ts" (will fail until Plan 02 creates that file — this is intentional RED state)
    3. Write helper: `async function run(name: string, fn: () => Promise<void>)` that wraps each test and reports pass/fail
    4. Group tests into sections matching the behavior list above
    5. Use a tmp directory `results/test-eval-tmp-${Date.now()}` for integration tests; always clean up in a finally block

    For the schema validation failure path test: call a helper `buildZeroScoreResult(validationError: string)` that mirrors what runEval will do — this tests the shape contract without needing a full runEval call.

    For JudgeResponse tests: import the Zod schema and call `.safeParse()` directly.

    For computeComposite tests: import the function and call it with literal inputs.

    Do NOT make any live Anthropic API calls. The judge call is tested via unit tests on the Zod schema and the score formula — the live integration is manual-only per VALIDATION.md.

    End the file with a summary: `console.log("All test-eval tests passed")` — only reached if no assertion throws.

    Commander --no-eval note: This plan does NOT test the CLI flag. That is tested manually per VALIDATION.md.
  </action>
  <verify>
    <automated>MISSING — Plan 02 must create src/eval.ts first. Running `npx tsx scripts/test-eval.ts` from repo root will fail with MODULE_NOT_FOUND until src/eval.ts exists. This RED state is expected and correct.</automated>
  </verify>
  <done>scripts/test-eval.ts exists, imports from src/eval.ts (which doesn't exist yet), and describes the full behavioral contract for EVAL-01 through EVAL-07. File is syntactically valid TypeScript.</done>
</task>

</tasks>

<verification>
After Plan 02 creates src/eval.ts, running `npx tsx scripts/test-eval.ts` must exit 0 with all assertions green.
The test file itself can be verified for syntax with: `npx tsc --noEmit scripts/test-eval.ts` (may need tsconfig path adjustments).
</verification>

<success_criteria>
- scripts/test-eval.ts exists and is syntactically valid TypeScript
- All 6 EVAL requirement behaviors are represented as assertions
- No live API calls in the test file
- Integration tests use a tmp results/ directory and clean up after themselves
</success_criteria>

<output>
After completion, create `.planning/phases/03-eval-engine/03-01-SUMMARY.md`
</output>
