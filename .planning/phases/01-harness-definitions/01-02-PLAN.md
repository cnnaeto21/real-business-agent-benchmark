---
phase: 01-harness-definitions
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - harnesses/inventory-optimization/harness.yaml
  - harnesses/inventory-optimization/prompt.md
  - harnesses/inventory-optimization/schema.ts
  - harnesses/inventory-optimization/rubric.md
  - harnesses/inventory-optimization/data/inventory.csv
  - harnesses/inventory-optimization/data/sales.csv
autonomous: true
requirements:
  - HRNS-01
  - HRNS-02
  - HRNS-03
  - HRNS-04
  - HRNS-05
  - HRNS-06
  - HRNS-07

must_haves:
  truths:
    - "harnesses/inventory-optimization/ contains all five required files plus data/ with two CSVs"
    - "harness.yaml declares version 1.0.0 and all required fields (data, prompt, output, eval, providers)"
    - "The inventory CSV data is anonymized — no real location names, dollar amounts scrubbed to relative values"
    - "schema.ts exports a named Zod v4 schema that z.toJSONSchema() converts without error"
    - "rubric.md contains score anchors at levels 1, 3, and 5 for all three dimensions"
    - "rubric.md contains the exact anti-verbosity instruction: 'Do not score higher simply because the response is longer'"
    - "prompt.md contains a data dictionary defining all non-obvious CSV column names"
  artifacts:
    - path: "harnesses/inventory-optimization/harness.yaml"
      provides: "Declarative harness spec"
      contains: "version: 1.0.0"
    - path: "harnesses/inventory-optimization/schema.ts"
      provides: "Zod v4 output schema generating valid JSON Schema"
      exports: ["InventoryRecommendation", "InventoryRecommendationType"]
    - path: "harnesses/inventory-optimization/rubric.md"
      provides: "Eval rubric with concrete score anchors"
      contains: "Do not score higher"
    - path: "harnesses/inventory-optimization/data/inventory.csv"
      provides: "Anonymized inventory snapshot (15-25 rows)"
    - path: "harnesses/inventory-optimization/data/sales.csv"
      provides: "Anonymized sales velocity data (15-25 rows)"
  key_links:
    - from: "harnesses/inventory-optimization/rubric.md"
      to: "harnesses/inventory-optimization/schema.ts"
      via: "completeness dimension references exact schema fields"
      pattern: "summary|recommendations|data_gaps"
    - from: "harnesses/inventory-optimization/harness.yaml"
      to: "harnesses/inventory-optimization/schema.ts"
      via: "output.schema field references schema.ts"
      pattern: "schema: schema.ts"
---

<objective>
Author the complete inventory-optimization harness: all five artifacts (harness.yaml, prompt.md, schema.ts, rubric.md, and two data CSVs).

Purpose: This is the most critical harness to get right — inventory optimization is the clearest domain for demonstrating differentiated model reasoning (restock/hold/reduce with quantities). The rubric authored here sets the quality bar for Plans 03 and 04.
Output: Six files forming a complete, self-contained harness package that produces valid Zod JSON Schema.
</objective>

<execution_context>
@/Users/obinnaeto/.claude/get-shit-done/workflows/execute-plan.md
@/Users/obinnaeto/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-harness-definitions/01-RESEARCH.md

<interfaces>
<!-- From 01-01-PLAN.md: harness.yaml pattern and Zod v4 schema pattern -->

harness.yaml required fields (from research):
```yaml
name: <string>
version: 1.0.0          # semver — HRNS-07
description: <string>
data:
  - file: data/<name>.csv
    inject_as: <template_var>
prompt:
  template: prompt.md
output:
  schema: schema.ts
eval:
  rubric: rubric.md
  judge_model: anthropic/claude-sonnet-4-6
  judge_temperature: 0
providers:
  - anthropic/claude-sonnet-4-6
  - openai/gpt-4o
  - google/gemini-1.5-pro
```

Zod v4 schema pattern (from research):
```typescript
import { z } from "zod";
export const MySchema = z.object({ ... });
export type MySchemaType = z.infer<typeof MySchema>;
// JSON Schema: z.toJSONSchema(MySchema)
```

Rubric required structure (from research):
- Anti-verbosity instruction at top
- Three dimensions: Actionability, Reasoning Transparency, Completeness
- Score anchors at 1, 3, and 5 for each dimension
- Completeness dimension MUST reference the actual schema field names
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Author harness.yaml, schema.ts, and CSV data files</name>
  <files>
    harnesses/inventory-optimization/harness.yaml,
    harnesses/inventory-optimization/schema.ts,
    harnesses/inventory-optimization/data/inventory.csv,
    harnesses/inventory-optimization/data/sales.csv
  </files>
  <behavior>
    - schema.ts exports InventoryRecommendation (ZodObject) — z.toJSONSchema(InventoryRecommendation) returns a valid object with type "object"
    - schema.ts exports InventoryRecommendationType as a TypeScript type
    - harness.yaml parses as valid YAML with all required fields present
    - inventory.csv has 15-25 rows with columns: sku, product_name, current_stock, par_level, unit_cost
    - sales.csv has 15-25 rows with columns: sku, velocity_7d, velocity_30d, last_restock_date
    - No real location names appear in any data file; dollar amounts are scaled relative values
  </behavior>
  <action>
Create harnesses/inventory-optimization/harness.yaml:
```yaml
name: inventory-optimization
version: 1.0.0
description: "Given current inventory levels and sales velocity data for a vending machine operation, recommend specific restocking actions for each SKU"

data:
  - file: data/inventory.csv
    inject_as: inventory_table
  - file: data/sales.csv
    inject_as: sales_table

prompt:
  template: prompt.md

output:
  schema: schema.ts

eval:
  rubric: rubric.md
  judge_model: anthropic/claude-sonnet-4-6
  judge_temperature: 0

providers:
  - anthropic/claude-sonnet-4-6
  - openai/gpt-4o
  - google/gemini-1.5-pro
```

Create harnesses/inventory-optimization/schema.ts using Zod v4:
```typescript
import { z } from "zod";

export const InventoryRecommendation = z.object({
  summary: z
    .string()
    .describe(
      "One-paragraph executive summary of overall inventory health and the highest-priority actions"
    ),
  recommendations: z
    .array(
      z.object({
        sku: z.string().describe("Product SKU identifier (e.g. DRINK-COLA-12OZ)"),
        action: z
          .enum(["restock", "hold", "reduce"])
          .describe("Recommended action for this SKU"),
        quantity: z
          .number()
          .int()
          .min(0)
          .describe(
            "Units to order; must be 0 if action is 'hold' or 'reduce'"
          ),
        rationale: z
          .string()
          .describe(
            "Explanation citing specific data values (e.g. '4-day stock at 12 units/day velocity')"
          ),
        urgency: z
          .enum(["immediate", "this-week", "next-cycle"])
          .describe("How soon the action must be taken"),
      })
    )
    .describe("One entry per SKU that requires a decision"),
  data_gaps: z
    .array(z.string())
    .describe(
      "Specific data points that are missing and would improve the recommendations"
    ),
});

export type InventoryRecommendationType = z.infer<typeof InventoryRecommendation>;
```

Create harnesses/inventory-optimization/data/inventory.csv with 20 anonymized rows. Use:
- SKUs named with category-product-size pattern: DRINK-COLA-12OZ, SNACK-CHIP-1OZ, etc. (no real brand names)
- par_level values representing real operational thresholds
- current_stock values creating a mix of: clearly below par (6-7 SKUs triggering restock), near par (6-7 SKUs borderline), well above par (6-7 SKUs to hold)
- unit_cost values that are scaled (multiply real values by 0.7 or similar fixed multiplier — exact multiplier does not matter as long as ratios are preserved)

Columns: sku, product_name, current_stock, par_level, unit_cost

Example rows (create 20 total with realistic variation):
```
sku,product_name,current_stock,par_level,unit_cost
DRINK-COLA-12OZ,Cola 12oz,4,20,0.84
DRINK-WATER-16OZ,Water 16oz,2,24,0.42
SNACK-CHIP-1OZ,Chips 1oz,18,20,0.56
DRINK-ENERGY-8OZ,Energy 8oz,0,12,1.26
SNACK-BAR-PROT,Protein Bar,8,10,1.19
...
```

Create harnesses/inventory-optimization/data/sales.csv with matching 20 rows (same SKUs):
- velocity_7d: units sold in last 7 days
- velocity_30d: units sold in last 30 days
- last_restock_date: use relative format like "2026-02-15" (shifted by fixed offset from real dates — pattern preserved)

Columns: sku, velocity_7d, velocity_30d, last_restock_date

The combination of current_stock and velocity_7d data should create compelling decision cases — e.g., DRINK-COLA-12OZ with current_stock=4 and velocity_7d=12 means ~2.5 days of stock left (clear immediate restock). Some SKUs should have high stock but also high velocity (hold). Some low velocity (reduce or hold). This creates differentiated reasoning opportunities for models.
  </action>
  <verify>
    <automated>npx tsx -e "import { z } from 'zod'; import { InventoryRecommendation } from './harnesses/inventory-optimization/schema.ts'; const s = z.toJSONSchema(InventoryRecommendation); console.assert(s.type === 'object', 'not object'); console.log('PASS: schema produces valid JSON Schema'); console.log(JSON.stringify(s).slice(0,100))"</automated>
  </verify>
  <done>z.toJSONSchema(InventoryRecommendation) returns a valid JSON Schema object with type "object"; harness.yaml is present with all required fields; both CSVs have 15-25 data rows with no real location/brand identifiers</done>
</task>

<task type="auto">
  <name>Task 2: Author prompt.md and rubric.md</name>
  <files>
    harnesses/inventory-optimization/prompt.md,
    harnesses/inventory-optimization/rubric.md
  </files>
  <action>
Create harnesses/inventory-optimization/prompt.md.

The system prompt must:
- Describe the task neutrally (analyze, recommend — no persona that implies a cost-cutting or growth bias)
- Include a data dictionary defining every non-obvious column name
- NOT imply what conclusion the agent should reach
- Instruct the agent to respond using the JSON schema (but not show the schema itself — the runner injects it via the provider's structured output API)

```markdown
# System Prompt

You are analyzing operational data for a vending machine business.
Your task is to review the current inventory status and recent sales velocity,
then provide specific restocking recommendations for each product that requires a decision.

## Data Dictionary

- **SKU**: Stock Keeping Unit — unique product identifier (e.g. DRINK-COLA-12OZ)
- **par_level**: The minimum stock quantity at which restocking should be triggered. Falling below par means the machine may run out before the next scheduled visit.
- **current_stock**: Units currently in the machine or warehouse at time of data export.
- **unit_cost**: Operator's cost per unit (not retail price). Used to assess reorder economics.
- **velocity_7d**: Units sold in the last 7 days. Divide current_stock by (velocity_7d / 7) to estimate days of remaining stock.
- **velocity_30d**: Units sold in the last 30 days. Use alongside velocity_7d to detect trends (rising/falling demand).
- **last_restock_date**: Date the SKU was last restocked. Combined with velocity data, indicates how quickly stock was depleted.

Respond using the structured output format. Do not add commentary outside the defined fields.

# User Message Template

Here is the current inventory status for each product:

```
{{inventory_table}}
```

Here is recent sales velocity data:

```
{{sales_table}}
```

Analyze this data and provide your restocking recommendations.
```

Create harnesses/inventory-optimization/rubric.md.

The rubric must satisfy HRNS-05 (concrete score anchors at 1, 3, and 5 per dimension) and HRNS-06 (explicit anti-verbosity instruction).

CRITICAL: The completeness dimension's score anchors must reference the actual schema field names: summary, recommendations (with sku, action, quantity, rationale, urgency), and data_gaps.

```markdown
# Eval Rubric: Inventory Optimization

## Anti-Verbosity Instruction

Do not score higher simply because the response is longer or more elaborate.
A concise recommendation that cites specific data values (e.g., "4-day stock at current velocity")
scores higher than a verbose response covering all possibilities without citing numbers.
Evaluate substance, not word count.

## Dimension 1: Actionability

Does the response provide recommendations specific enough to execute without further clarification?

**Score 5:** Every recommendation in the `recommendations` array specifies a concrete SKU identifier,
a definitive action (restock/hold/reduce), an exact quantity (non-zero for restock, 0 for hold/reduce),
and a clear urgency level. A warehouse manager could place an order and schedule a machine visit
from this output alone, without asking any follow-up questions.

**Score 3:** Recommendations identify which products need attention and the action type, but are vague
about quantities (e.g., "order some more" or "quantity TBD") or urgency (e.g., "soon" without a
defined timeframe). The response is directionally correct but not operationally complete.

**Score 1:** Recommendations are general observations or restatements of the data
(e.g., "several products are low on stock") with no actionable specifics tied to individual SKUs.
A manager cannot act without significant additional analysis.

## Dimension 2: Reasoning Transparency

Does the response show its reasoning — which specific data values drove which conclusions?

**Score 5:** Each recommendation in the `rationale` field traces its conclusion to exact values
from the provided data (e.g., "current_stock=4 / velocity_7d=12 = 2.3 days remaining, below
the 14-day restock threshold"). Calculations or comparisons are explicit and reproducible.

**Score 3:** Recommendations reference data trends in general terms ("high-velocity product",
"stock is running low") but do not cite specific numeric values from the CSV. A reader can
infer what data was used but cannot verify the calculation.

**Score 1:** Conclusions appear without any visible connection to the provided data.
The response could have been generated without access to the inventory or sales data at all.
Rationale fields are empty, generic, or contain only assertions ("this product needs restocking").

## Dimension 3: Completeness

Does the output populate all required schema fields meaningfully and cover the full set of SKUs
requiring a decision?

**Score 5:** The `summary` field synthesizes overall inventory health in a way that adds context
beyond listing individual recommendations. The `recommendations` array covers every SKU that
falls below or near par_level (no obvious candidates are omitted). The `data_gaps` field
identifies at least one genuine missing data point (e.g., expiration dates, machine location
visit schedules) that would materially improve the analysis, or explicitly states none exist.

**Score 3:** Required fields (`summary`, `recommendations`, `data_gaps`) are present but some are
superficial: the summary restates the prompt rather than synthesizing findings, `data_gaps` is an
empty array when obvious gaps exist (such as no expiry or margin data), or the recommendations
list omits borderline SKUs that warranted a decision.

**Score 1:** One or more required schema fields (`summary`, `recommendations`, or `data_gaps`) are
missing, null, or contain placeholder content. The recommendations array is empty or covers fewer
than half of the SKUs that are at or below par_level.
```
  </action>
  <verify>
    <automated>grep -c "Do not score higher" harnesses/inventory-optimization/rubric.md && grep -c "Score 5" harnesses/inventory-optimization/rubric.md && grep -c "Score 3" harnesses/inventory-optimization/rubric.md && grep -c "Score 1" harnesses/inventory-optimization/rubric.md && echo "PASS: anti-verbosity and all score anchors present"</automated>
  </verify>
  <done>prompt.md exists with data dictionary covering all CSV column names; rubric.md exists with anti-verbosity instruction and score anchors at 1/3/5 for all three dimensions; completeness dimension references summary, recommendations, and data_gaps by name</done>
</task>

</tasks>

<verification>
- `npx tsx -e "import { z } from 'zod'; import { InventoryRecommendation } from './harnesses/inventory-optimization/schema.ts'; const s = z.toJSONSchema(InventoryRecommendation); console.log(s.type)"` prints "object"
- `ls harnesses/inventory-optimization/` shows: harness.yaml, prompt.md, schema.ts, rubric.md, data/
- `ls harnesses/inventory-optimization/data/` shows: inventory.csv, sales.csv
- `grep "Do not score higher" harnesses/inventory-optimization/rubric.md` returns a match
- `grep "version: 1.0.0" harnesses/inventory-optimization/harness.yaml` returns a match
- Both CSVs have 15+ data rows: `wc -l harnesses/inventory-optimization/data/*.csv`
</verification>

<success_criteria>
- z.toJSONSchema(InventoryRecommendation) succeeds without error
- harness.yaml contains version, data, prompt, output, eval, providers fields
- rubric.md has anti-verbosity instruction + score anchors at 1, 3, 5 for Actionability + Reasoning Transparency + Completeness
- No real brand names, location names, or unscaled dollar amounts in CSVs
- prompt.md data dictionary defines: SKU, par_level, current_stock, unit_cost, velocity_7d, velocity_30d, last_restock_date
</success_criteria>

<output>
After completion, create `.planning/phases/01-harness-definitions/01-02-SUMMARY.md`
</output>
