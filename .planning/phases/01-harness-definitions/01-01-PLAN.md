---
phase: 01-harness-definitions
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - scripts/validate-schemas.ts
  - harnesses/inventory-optimization/.gitkeep
  - harnesses/pricing-strategy/.gitkeep
  - harnesses/financial-forecasting/.gitkeep
  - docs/.gitkeep
autonomous: true
requirements:
  - HRNS-01
  - HRNS-04

must_haves:
  truths:
    - "Three harness directories exist under harnesses/"
    - "docs/ directory exists for the shared judge prompt"
    - "package.json declares zod@^4.3.6 and tsx as dev dependency"
    - "scripts/validate-schemas.ts can be run with npx tsx and validates all three harness schemas"
  artifacts:
    - path: "package.json"
      provides: "Project dependency declaration with zod and tsx"
      contains: "zod"
    - path: "scripts/validate-schemas.ts"
      provides: "Automated schema validation — runs z.toJSONSchema() for all three harnesses"
      min_lines: 20
    - path: "harnesses/inventory-optimization"
      provides: "Harness directory (scaffold, content added in Plans 02-04)"
    - path: "harnesses/pricing-strategy"
      provides: "Harness directory scaffold"
    - path: "harnesses/financial-forecasting"
      provides: "Harness directory scaffold"
  key_links:
    - from: "scripts/validate-schemas.ts"
      to: "harnesses/*/schema.ts"
      via: "dynamic import of each schema file"
      pattern: "import.*schema"
---

<objective>
Bootstrap the project: create package.json with Zod v4, scaffold the three harness directories and docs/, and author the validation script that all later plans depend on for automated verification.

Purpose: Plans 02-04 (harness authoring) need the directory structure to exist and need a working validate-schemas.ts script to satisfy their automated verify steps. This plan creates those foundations.
Output: package.json, tsconfig.json, harnesses/ directory tree (three dirs), docs/ dir, scripts/validate-schemas.ts
</objective>

<execution_context>
@/Users/obinnaeto/.claude/get-shit-done/workflows/execute-plan.md
@/Users/obinnaeto/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-harness-definitions/01-RESEARCH.md
@.planning/phases/01-harness-definitions/01-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize package.json and tsconfig.json</name>
  <files>package.json, tsconfig.json</files>
  <action>
Create package.json at repo root with:
- `"name": "rbab"`, `"version": "0.1.0"`, `"private": true`, `"type": "module"`
- `"scripts": { "validate": "tsx scripts/validate-schemas.ts" }`
- `"dependencies": { "zod": "^4.3.6" }`
- `"devDependencies": { "typescript": "^5.4.0", "tsx": "^4.15.0" }`

Create tsconfig.json at repo root with:
```json
{
  "compilerOptions": {
    "target": "ES2022",
    "module": "ESNext",
    "moduleResolution": "bundler",
    "esModuleInterop": true,
    "strict": true,
    "skipLibCheck": true,
    "outDir": "dist"
  },
  "include": ["harnesses/**/*.ts", "scripts/**/*.ts"]
}
```

Run `npm install` after creating package.json to install dependencies.

Do NOT install `zod-to-json-schema` — it is EOL as of November 2025. Use Zod v4's native `z.toJSONSchema()` instead.
  </action>
  <verify>
    <automated>node -e "const p = JSON.parse(require('fs').readFileSync('package.json','utf8')); console.assert(p.dependencies.zod, 'zod missing'); console.log('ok')"</automated>
  </verify>
  <done>package.json exists with zod@^4.3.6 in dependencies; npm install completes without errors; node_modules/zod exists</done>
</task>

<task type="auto">
  <name>Task 2: Scaffold harness directories and author validate-schemas.ts</name>
  <files>
    harnesses/inventory-optimization/.gitkeep,
    harnesses/inventory-optimization/data/.gitkeep,
    harnesses/pricing-strategy/.gitkeep,
    harnesses/pricing-strategy/data/.gitkeep,
    harnesses/financial-forecasting/.gitkeep,
    harnesses/financial-forecasting/data/.gitkeep,
    docs/.gitkeep,
    scripts/validate-schemas.ts
  </files>
  <action>
Create the directory tree with .gitkeep files so directories are tracked by git:
- harnesses/inventory-optimization/
- harnesses/inventory-optimization/data/
- harnesses/pricing-strategy/
- harnesses/pricing-strategy/data/
- harnesses/financial-forecasting/
- harnesses/financial-forecasting/data/
- docs/

Create scripts/validate-schemas.ts. This script is the primary automated verification tool for Plans 02-04. It must:

1. Import each harness schema by dynamic import (after Plans 02-04 create the schema.ts files)
2. Call `z.toJSONSchema()` on the exported schema object
3. Assert the result is a valid object with a `type` property
4. Print the JSON schema summary and "PASS" or throw and exit non-zero on failure

```typescript
// scripts/validate-schemas.ts
import { z } from "zod";
import { readFileSync } from "fs";

const HARNESSES = [
  "inventory-optimization",
  "pricing-strategy",
  "financial-forecasting",
] as const;

async function validateHarness(name: string): Promise<void> {
  const schemaPath = new URL(
    `../harnesses/${name}/schema.ts`,
    import.meta.url
  ).pathname;

  // Dynamic import — schema.ts must export a named Zod schema
  const mod = await import(schemaPath);

  // Find the first exported Zod schema (any ZodObject)
  const schemaExport = Object.values(mod).find(
    (v): v is z.ZodTypeAny => v instanceof z.ZodType
  );

  if (!schemaExport) {
    throw new Error(`No ZodType export found in harnesses/${name}/schema.ts`);
  }

  const jsonSchema = z.toJSONSchema(schemaExport);

  if (!jsonSchema || typeof jsonSchema !== "object") {
    throw new Error(`z.toJSONSchema() returned invalid result for ${name}`);
  }

  console.log(`PASS ${name}: ${JSON.stringify(jsonSchema).slice(0, 80)}...`);
}

const target = process.argv[2]; // optional: validate single harness
const toValidate = target ? [target] : HARNESSES;

let failed = 0;
for (const name of toValidate) {
  try {
    await validateHarness(name);
  } catch (err) {
    console.error(`FAIL ${name}: ${(err as Error).message}`);
    failed++;
  }
}

if (failed > 0) {
  console.error(`\n${failed} harness(es) failed validation`);
  process.exit(1);
}
console.log(`\nAll ${toValidate.length} harness(es) passed.`);
```

NOTE: This script will fail until Plans 02-04 create the actual schema.ts files. That is expected. The verify step for THIS plan only checks the script file exists and is valid TypeScript — not that it passes (schemas don't exist yet).
  </action>
  <verify>
    <automated>ls harnesses/inventory-optimization harnesses/pricing-strategy harnesses/financial-forecasting docs scripts/validate-schemas.ts && echo "PASS: all directories and script exist"</automated>
  </verify>
  <done>Three harness directories with data/ subdirs exist; docs/ exists; scripts/validate-schemas.ts exists and contains the validate loop; .gitkeep files ensure git tracks empty dirs</done>
</task>

</tasks>

<verification>
- package.json contains `"zod": "^4.3.6"` — confirm with `cat package.json | grep zod`
- node_modules/zod exists after npm install
- All three harness directories exist: `ls harnesses/`
- scripts/validate-schemas.ts exists: `ls scripts/`
- docs/ exists: `ls docs/`
</verification>

<success_criteria>
- `npm install` runs without errors
- `ls harnesses/` shows all three harness subdirectories
- `cat package.json` shows zod in dependencies (not zod-to-json-schema)
- scripts/validate-schemas.ts is valid TypeScript (no parse errors when checked with `npx tsx --noEmit scripts/validate-schemas.ts` — it will error on missing schemas, not on syntax)
</success_criteria>

<output>
After completion, create `.planning/phases/01-harness-definitions/01-01-SUMMARY.md`
</output>
