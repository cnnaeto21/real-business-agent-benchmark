---
phase: 01-harness-definitions
plan: 05
type: execute
wave: 3
depends_on:
  - 01-02
  - 01-03
  - 01-04
files_modified:
  - docs/judge-prompt.md
autonomous: true
requirements:
  - EVAL-02

must_haves:
  truths:
    - "docs/judge-prompt.md exists and is committed to the repo"
    - "The judge prompt contains all five required sections: role, subject output placeholder, rubric placeholder, anti-bias instructions, required JSON output format"
    - "The judge prompt's required output format matches the three scoring dimensions: actionability, reasoning_transparency, completeness"
    - "The anti-verbosity instruction appears explicitly in the judge prompt"
    - "Running npx tsx scripts/validate-schemas.ts returns PASS for all three harnesses"
  artifacts:
    - path: "docs/judge-prompt.md"
      provides: "Shared judge prompt for EVAL-02 — used by eval engine for all reference runs"
      contains: "actionability"
    - path: "scripts/validate-schemas.ts"
      provides: "Validation script confirming all three schemas produce valid JSON Schema"
  key_links:
    - from: "docs/judge-prompt.md"
      to: "harnesses/*/rubric.md"
      via: "{{rubric}} placeholder injected at eval runtime"
      pattern: "\\{\\{rubric\\}\\}"
    - from: "docs/judge-prompt.md"
      to: "harnesses/*/schema.ts"
      via: "required output format matches three dimensions scored across all harnesses"
      pattern: "actionability.*reasoning_transparency.*completeness"
---

<objective>
Author docs/judge-prompt.md (EVAL-02) and run the final cross-harness schema validation to confirm all three harnesses pass.

Purpose: The judge prompt is a harness artifact that must be locked before the eval engine is built. It must be stable, committed, and published in the repo (REF-03 requires it at docs/judge-prompt.md for all nine reference runs). This plan also confirms the entire phase succeeds by running validate-schemas.ts against all three harnesses.
Output: docs/judge-prompt.md committed; npx tsx scripts/validate-schemas.ts passes for all three harnesses.
</objective>

<execution_context>
@/Users/obinnaeto/.claude/get-shit-done/workflows/execute-plan.md
@/Users/obinnaeto/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-harness-definitions/01-RESEARCH.md

<interfaces>
<!-- Judge prompt structure from 01-RESEARCH.md Pattern 5 -->

Required sections:
1. Role and task description (judge is scoring, not helping)
2. Subject model output placeholder: {{model_output}}
3. Rubric placeholder: {{rubric}} (injected at runtime from each harness's rubric.md)
4. Anti-bias instructions (verbosity, self-enhancement, formatting)
5. Required output format — structured JSON with score + rationale per dimension

Three dimensions (must match rubric dimension names across all three harnesses):
- actionability
- reasoning_transparency
- completeness

Each dimension returns: { "score": <1-5>, "rationale": "<one sentence citing evidence>" }

From 01-RESEARCH.md example judge prompt structure:
```markdown
# Judge Prompt
You are an impartial evaluator scoring the quality of an AI agent's response...
## Subject Model Output
{{model_output}}
## Evaluation Rubric
{{rubric}}
## Scoring Instructions
...anti-verbosity...
## Required Output Format
Return valid JSON...
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author docs/judge-prompt.md</name>
  <files>docs/judge-prompt.md</files>
  <action>
Create docs/judge-prompt.md. This file is committed to the repo and is the judge prompt referenced by EVAL-02 and REF-03.

The eval engine will inject {{model_output}} and {{rubric}} at runtime. The three dimension names (actionability, reasoning_transparency, completeness) must appear in both the scoring instructions and the required output format, and must match the three dimensions authored in every harness's rubric.md.

```markdown
# RBAB Judge Prompt

**Version:** 1.0.0
**Judge Model:** anthropic/claude-sonnet-4-6 (temperature 0)
**Used for:** All RBAB reference runs and community runs

---

You are an impartial evaluator scoring the quality of an AI agent's response to a business operations task.
You are NOT the agent. Your role is to assess the response objectively against the rubric provided below.
Do not attempt to solve the task yourself — evaluate only what the subject model produced.

## Subject Model Output

The following is the response produced by the model being evaluated:

```
{{model_output}}
```

## Evaluation Rubric

Score this response against the following rubric. Each dimension has explicit anchors at 1, 3, and 5.

```
{{rubric}}
```

## Scoring Instructions

Apply these instructions to every dimension before assigning a score:

1. **Score against the anchor, not your intuition.** Use the rubric's explicit descriptions for scores 1, 3, and 5. Interpolate for 2 and 4 only when the response clearly falls between two described anchors.

2. **Do not score higher simply because the response is longer or more detailed.** A concise, specific answer that cites exact data values scores higher than a verbose response that discusses many considerations without grounding conclusions in the provided data. Length is not a proxy for quality.

3. **Do not infer quality from formatting.** Bullet points, numbered lists, headers, and bold text do not affect the score. Evaluate the substance of the content within those formatting choices.

4. **Do not reward confidence without evidence.** A response that asserts conclusions authoritatively without citing supporting data should score lower on Reasoning Transparency than a response that hedges appropriately while citing its evidence.

5. **Score each dimension independently.** A response can score 5 on Completeness and 1 on Actionability. Do not let a strong score on one dimension inflate scores on others.

6. **Provide a one-sentence rationale that cites specific evidence.** Do not write "the response was comprehensive" — write "the response populated all five schema fields including data_gaps, which identified missing expiry date data." The rationale must reference something observable in the model output.

## Required Output Format

Return valid JSON only. Do not add any text before or after the JSON object.

```json
{
  "actionability": {
    "score": <integer 1-5>,
    "rationale": "<one sentence citing specific observable evidence from the model output>"
  },
  "reasoning_transparency": {
    "score": <integer 1-5>,
    "rationale": "<one sentence citing specific observable evidence from the model output>"
  },
  "completeness": {
    "score": <integer 1-5>,
    "rationale": "<one sentence citing specific observable evidence from the model output>"
  }
}
```

All three fields are required. `score` must be an integer between 1 and 5 inclusive.
`rationale` must be a non-empty string.

---

*This prompt is published at `docs/judge-prompt.md` in the RBAB repository.
All reference runs use this prompt without modification. Changes to this file
require re-scoring all existing reference runs.*
```
  </action>
  <verify>
    <automated>grep -c "{{model_output}}" docs/judge-prompt.md && grep -c "{{rubric}}" docs/judge-prompt.md && grep -c "actionability" docs/judge-prompt.md && grep -c "reasoning_transparency" docs/judge-prompt.md && grep -c "completeness" docs/judge-prompt.md && grep -c "Do not score higher" docs/judge-prompt.md && echo "PASS: all required judge prompt sections present"</automated>
  </verify>
  <done>docs/judge-prompt.md exists with all five required sections; both placeholders ({{model_output}}, {{rubric}}) are present; all three scoring dimensions are named in the output format; anti-verbosity instruction is explicit</done>
</task>

<task type="auto">
  <name>Task 2: Run cross-harness schema validation</name>
  <files></files>
  <action>
Run the validate-schemas.ts script (created in Plan 01) against all three harnesses. This confirms HRNS-04 for all three harnesses and verifies Zod v4 is correctly installed.

Run:
```bash
npx tsx scripts/validate-schemas.ts
```

Expected output: Three "PASS" lines (one per harness) and "All 3 harness(es) passed."

If any harness fails:
- "No ZodType export found" — the schema.ts file does not export a Zod schema object; fix the export name
- "z.toJSONSchema() returned invalid result" — the schema definition has an incompatible Zod v4 construct; review the schema.ts and correct the field definition

Do NOT proceed until all three harnesses pass. Fix any schema issues in the respective harness schema.ts files before declaring this task complete.

Also run a quick file existence check to confirm all harness artifacts from Plans 02-04 are present:
```bash
for harness in inventory-optimization pricing-strategy financial-forecasting; do
  echo "=== $harness ==="
  ls harnesses/$harness/
  ls harnesses/$harness/data/
done
echo "=== docs ==="
ls docs/
```

This is a verification-only task — no files are created or modified. The output of these commands constitutes the done criteria.
  </action>
  <verify>
    <automated>npx tsx scripts/validate-schemas.ts</automated>
  </verify>
  <done>npx tsx scripts/validate-schemas.ts exits 0 with "All 3 harness(es) passed." output; all harness directories contain the five required files plus data/ with two CSVs each; docs/judge-prompt.md exists</done>
</task>

</tasks>

<verification>
- `cat docs/judge-prompt.md | grep "{{model_output}}"` returns match
- `cat docs/judge-prompt.md | grep "{{rubric}}"` returns match
- `cat docs/judge-prompt.md | grep "reasoning_transparency"` returns match
- `npx tsx scripts/validate-schemas.ts` exits 0 with PASS for all 3 harnesses
- `ls harnesses/*/rubric.md` returns 3 files
- `grep "Do not score higher" harnesses/*/rubric.md` returns 3 matches (one per harness)
- `grep "version: 1.0.0" harnesses/*/harness.yaml` returns 3 matches
</verification>

<success_criteria>
Phase 1 complete when ALL of the following are true:
1. Three harness directories exist with all five artifacts each (harness.yaml, prompt.md, schema.ts, rubric.md, data/ with two CSVs)
2. Each rubric.md contains score anchors at 1/3/5 for all three dimensions AND the anti-verbosity instruction
3. Each harness.yaml has `version: 1.0.0` and all required fields
4. `npx tsx scripts/validate-schemas.ts` exits 0 (all three Zod schemas produce valid JSON Schema via z.toJSONSchema())
5. docs/judge-prompt.md exists with both runtime placeholders, all three dimension names, and anti-verbosity instruction
</success_criteria>

<output>
After completion, create `.planning/phases/01-harness-definitions/01-05-SUMMARY.md`
</output>
