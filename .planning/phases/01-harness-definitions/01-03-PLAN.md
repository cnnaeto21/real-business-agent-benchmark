---
phase: 01-harness-definitions
plan: 03
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - harnesses/pricing-strategy/harness.yaml
  - harnesses/pricing-strategy/prompt.md
  - harnesses/pricing-strategy/schema.ts
  - harnesses/pricing-strategy/rubric.md
  - harnesses/pricing-strategy/data/products.csv
  - harnesses/pricing-strategy/data/sales.csv
autonomous: true
requirements:
  - HRNS-01
  - HRNS-02
  - HRNS-03
  - HRNS-04
  - HRNS-05
  - HRNS-06
  - HRNS-07

must_haves:
  truths:
    - "harnesses/pricing-strategy/ contains all five required files plus data/ with two CSVs"
    - "harness.yaml declares version 1.0.0 and all required fields"
    - "The pricing CSV data is anonymized — SKU codes instead of brand names, scaled prices preserving ratios"
    - "schema.ts exports a named Zod v4 schema that z.toJSONSchema() converts without error"
    - "rubric.md contains score anchors at levels 1, 3, and 5 for all three dimensions"
    - "rubric.md contains the exact anti-verbosity instruction: 'Do not score higher simply because the response is longer'"
    - "prompt.md contains a data dictionary defining all pricing-domain column names"
  artifacts:
    - path: "harnesses/pricing-strategy/harness.yaml"
      provides: "Declarative harness spec"
      contains: "version: 1.0.0"
    - path: "harnesses/pricing-strategy/schema.ts"
      provides: "Zod v4 output schema for price recommendations"
      exports: ["PricingRecommendation", "PricingRecommendationType"]
    - path: "harnesses/pricing-strategy/rubric.md"
      provides: "Eval rubric with concrete score anchors"
      contains: "Do not score higher"
    - path: "harnesses/pricing-strategy/data/products.csv"
      provides: "Anonymized product catalog with pricing data (15-25 rows)"
    - path: "harnesses/pricing-strategy/data/sales.csv"
      provides: "Anonymized sales velocity by price point (15-25 rows)"
  key_links:
    - from: "harnesses/pricing-strategy/rubric.md"
      to: "harnesses/pricing-strategy/schema.ts"
      via: "completeness dimension references exact schema fields"
      pattern: "summary|recommendations|market_observations"
    - from: "harnesses/pricing-strategy/harness.yaml"
      to: "harnesses/pricing-strategy/schema.ts"
      via: "output.schema field"
      pattern: "schema: schema.ts"
---

<objective>
Author the complete pricing-strategy harness: all five artifacts (harness.yaml, prompt.md, schema.ts, rubric.md, and two data CSVs) for the pricing decisions domain.

Purpose: Pricing strategy requires a different reasoning pattern than inventory — the agent must weigh price elasticity, margin, and competitive positioning rather than simple restock thresholds. The schema must capture price change direction and confidence, not just action types.
Output: Six files forming a complete, self-contained pricing-strategy harness package.
</objective>

<execution_context>
@/Users/obinnaeto/.claude/get-shit-done/workflows/execute-plan.md
@/Users/obinnaeto/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-harness-definitions/01-RESEARCH.md

<interfaces>
<!-- harness.yaml pattern and Zod v4 schema pattern — same structure as Plan 02 -->

harness.yaml required fields:
```yaml
name: pricing-strategy
version: 1.0.0
description: <string>
data:
  - file: data/products.csv
    inject_as: products_table
  - file: data/sales.csv
    inject_as: sales_table
prompt:
  template: prompt.md
output:
  schema: schema.ts
eval:
  rubric: rubric.md
  judge_model: anthropic/claude-sonnet-4-6
  judge_temperature: 0
providers:
  - anthropic/claude-sonnet-4-6
  - openai/gpt-4o
  - google/gemini-1.5-pro
```

Zod v4 schema pattern:
```typescript
import { z } from "zod";
export const PricingRecommendation = z.object({ ... });
export type PricingRecommendationType = z.infer<typeof PricingRecommendation>;
```

Research example for pricing schema (from 01-RESEARCH.md):
```typescript
export const PricingRecommendation = z.object({
  summary: z.string().describe("Overall pricing assessment"),
  recommendations: z.array(z.object({
    sku: z.string(),
    current_price: z.number().describe("Current price in dollars"),
    recommended_price: z.number().describe("Recommended price in dollars"),
    direction: z.enum(["increase", "decrease", "hold"]),
    rationale: z.string().describe("Data-driven justification citing specific sales figures"),
    confidence: z.enum(["high", "medium", "low"]),
  })),
  market_observations: z.array(z.string()).describe("Broader pricing dynamics observed in the data"),
});
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Author harness.yaml, schema.ts, and CSV data files</name>
  <files>
    harnesses/pricing-strategy/harness.yaml,
    harnesses/pricing-strategy/schema.ts,
    harnesses/pricing-strategy/data/products.csv,
    harnesses/pricing-strategy/data/sales.csv
  </files>
  <behavior>
    - schema.ts exports PricingRecommendation (ZodObject) — z.toJSONSchema(PricingRecommendation) returns a valid object with type "object"
    - schema.ts exports PricingRecommendationType as a TypeScript type
    - harness.yaml parses as valid YAML with all required fields
    - products.csv has 15-25 rows with columns: sku, product_name, current_price, cost_per_unit, category
    - sales.csv has 15-25 rows with columns: sku, units_sold_30d, revenue_30d, price_changes_90d (number of price changes), avg_price_90d
    - No real brand names; prices are scaled (multiply real prices by a fixed ratio, e.g., 0.85, preserving margin relationships)
  </behavior>
  <action>
Create harnesses/pricing-strategy/harness.yaml:
```yaml
name: pricing-strategy
version: 1.0.0
description: "Given product sales velocity, current prices, and cost data for a vending machine operation, recommend price adjustments that optimize revenue while maintaining competitive positioning"

data:
  - file: data/products.csv
    inject_as: products_table
  - file: data/sales.csv
    inject_as: sales_table

prompt:
  template: prompt.md

output:
  schema: schema.ts

eval:
  rubric: rubric.md
  judge_model: anthropic/claude-sonnet-4-6
  judge_temperature: 0

providers:
  - anthropic/claude-sonnet-4-6
  - openai/gpt-4o
  - google/gemini-1.5-pro
```

Create harnesses/pricing-strategy/schema.ts using Zod v4:
```typescript
import { z } from "zod";

export const PricingRecommendation = z.object({
  summary: z
    .string()
    .describe(
      "Executive summary of the overall pricing situation and highest-impact recommendations"
    ),
  recommendations: z
    .array(
      z.object({
        sku: z.string().describe("Product SKU identifier"),
        current_price: z
          .number()
          .describe("Current retail price in dollars (from the provided data)"),
        recommended_price: z
          .number()
          .describe("Recommended new retail price in dollars"),
        direction: z
          .enum(["increase", "decrease", "hold"])
          .describe("Direction of the price change"),
        rationale: z
          .string()
          .describe(
            "Data-driven justification citing specific sales figures, margin data, or velocity observations"
          ),
        confidence: z
          .enum(["high", "medium", "low"])
          .describe(
            "Confidence in the recommendation given available data quality"
          ),
      })
    )
    .describe("One entry per SKU requiring a pricing decision"),
  market_observations: z
    .array(z.string())
    .describe(
      "Broader patterns observed across the product catalog that inform the pricing strategy"
    ),
});

export type PricingRecommendationType = z.infer<typeof PricingRecommendation>;
```

Create harnesses/pricing-strategy/data/products.csv with 20 anonymized rows.
Columns: sku, product_name, current_price, cost_per_unit, category

Use SKU naming: DRINK-*, SNACK-*, CANDY-* prefixes. No real brand names (e.g., "Energy Drink 8oz" not "Red Bull").

Design the data to create interesting pricing decisions:
- Several SKUs with thin margins (current_price barely above cost_per_unit) — candidates for price increase
- Several SKUs with wide margins that are also high velocity — candidates for holding or slight increase
- One or two SKUs with high prices but low velocity — candidates for decrease to drive volume
- Scale all prices by a fixed multiplier (e.g., multiply real prices by 0.85) — preserve relative margins

Example rows (create 20 total):
```
sku,product_name,current_price,cost_per_unit,category
DRINK-COLA-12OZ,Cola 12oz,1.59,0.84,beverage
DRINK-ENERGY-8OZ,Energy 8oz,2.49,1.26,beverage
DRINK-WATER-16OZ,Water 16oz,1.29,0.42,beverage
SNACK-CHIP-1OZ,Chips 1oz,1.29,0.56,snack
CANDY-CHOC-1OZ,Chocolate Bar 1oz,1.49,0.63,candy
...
```

Create harnesses/pricing-strategy/data/sales.csv with matching 20 rows (same SKUs).
Columns: sku, units_sold_30d, revenue_30d, price_changes_90d, avg_price_90d

- revenue_30d = units_sold_30d × current_price (consistent with products.csv prices)
- price_changes_90d: 0-3 for most products (shows pricing history)
- avg_price_90d: may differ from current_price for products with recent changes — this creates reasoning opportunities about price elasticity
  </action>
  <verify>
    <automated>npx tsx -e "import { z } from 'zod'; import { PricingRecommendation } from './harnesses/pricing-strategy/schema.ts'; const s = z.toJSONSchema(PricingRecommendation); console.assert(s.type === 'object', 'not object'); console.log('PASS: pricing schema produces valid JSON Schema')"</automated>
  </verify>
  <done>z.toJSONSchema(PricingRecommendation) returns a valid JSON Schema object; harness.yaml is present with all required fields; both CSVs have 15-25 data rows with no real brand names and scaled prices</done>
</task>

<task type="auto">
  <name>Task 2: Author prompt.md and rubric.md</name>
  <files>
    harnesses/pricing-strategy/prompt.md,
    harnesses/pricing-strategy/rubric.md
  </files>
  <action>
Create harnesses/pricing-strategy/prompt.md.

The system prompt must:
- Describe the task neutrally (no persona implying growth-at-all-costs or margin-maximization bias)
- Include a data dictionary for all pricing-domain column names
- NOT imply what conclusion the agent should reach

```markdown
# System Prompt

You are analyzing sales and pricing data for a vending machine business.
Your task is to review current product prices, cost data, and sales velocity,
then recommend specific price adjustments that are supported by the data.

## Data Dictionary

- **SKU**: Stock Keeping Unit — unique product identifier
- **current_price**: Current retail price charged to customers (in dollars)
- **cost_per_unit**: Operator's cost to purchase one unit (in dollars). Margin = current_price - cost_per_unit.
- **category**: Product category (beverage, snack, candy, etc.)
- **units_sold_30d**: Total units sold in the last 30 days across all machines
- **revenue_30d**: Total revenue generated by this SKU in the last 30 days (units × average price)
- **price_changes_90d**: Number of times this SKU's price was changed in the last 90 days
- **avg_price_90d**: Average selling price over the last 90 days. Compare to current_price to identify recent changes.

Respond using the structured output format. Do not add commentary outside the defined fields.

# User Message Template

Here is the current product catalog with pricing and cost data:

```
{{products_table}}
```

Here is recent sales performance data:

```
{{sales_table}}
```

Analyze this data and provide your pricing recommendations.
```

Create harnesses/pricing-strategy/rubric.md.

CRITICAL: The completeness dimension MUST reference the actual schema field names: summary, recommendations (with sku, current_price, recommended_price, direction, rationale, confidence), and market_observations.

```markdown
# Eval Rubric: Pricing Strategy

## Anti-Verbosity Instruction

Do not score higher simply because the response is longer or more elaborate.
A concise recommendation citing specific margin percentages and velocity figures
scores higher than a verbose response that discusses all possible pricing frameworks
without grounding conclusions in the provided data.
Evaluate substance and specificity, not word count.

## Dimension 1: Actionability

Does the response provide pricing recommendations specific enough to implement?

**Score 5:** Every recommendation specifies an exact `recommended_price` (not a range or direction only),
identifies the `direction` of change (increase/decrease/hold), and provides a `confidence` rating.
A pricing manager could update prices in the point-of-sale system immediately from this output.

**Score 3:** Recommendations identify which SKUs need attention and the general direction (increase or
decrease) but give only approximate guidance on the new price (e.g., "raise by roughly 10-15%")
rather than a specific dollar amount. The output is directionally useful but not immediately actionable.

**Score 1:** Recommendations describe general pricing principles (e.g., "increase prices on popular items")
without tying advice to specific SKUs or providing any price point guidance. Cannot be acted on
without significant additional analysis.

## Dimension 2: Reasoning Transparency

Does the response show which data drove which pricing decisions?

**Score 5:** Each `rationale` field traces the price recommendation to specific values from the data
(e.g., "margin is 47% vs. category average of 38%; velocity of 45 units/month supports a $0.20 increase
without demand risk"). Margin calculations reference actual cost_per_unit and current_price values.

**Score 3:** Rationale fields reference data categories in general terms ("this is a high-margin product",
"sales velocity is strong") without citing the specific numbers that support the claim. A reader
can infer what data was used but cannot independently verify the reasoning.

**Score 1:** Rationale fields are absent, generic, or circular ("price should increase because revenue
would be higher"). No connection is made between the recommendation and specific data values
from the provided tables.

## Dimension 3: Completeness

Does the output populate all required schema fields meaningfully and provide strategic context?

**Score 5:** The `summary` field provides a strategic overview identifying the top revenue opportunities
and any systemic pricing patterns (e.g., beverage margins lagging snack margins). The `recommendations`
array covers all SKUs where a defensible case for price change exists. The `market_observations` field
identifies at least two cross-catalog patterns (e.g., price sensitivity differences across categories,
correlation between price changes and velocity shifts) that go beyond restating individual recommendations.

**Score 3:** Required fields (`summary`, `recommendations`, `market_observations`) are present but
superficial: the summary restates individual recommendations without synthesizing patterns, or
`market_observations` contains only one generic observation. Some SKUs with obvious pricing opportunities
are omitted from the recommendations.

**Score 1:** One or more required schema fields (`summary`, `recommendations`, or `market_observations`)
are missing, null, empty arrays, or contain placeholder text. The recommendations array covers fewer
than half of the SKUs that warrant a pricing decision based on the provided data.
```
  </action>
  <verify>
    <automated>grep -c "Do not score higher" harnesses/pricing-strategy/rubric.md && grep -c "Score 5" harnesses/pricing-strategy/rubric.md && grep -c "Score 3" harnesses/pricing-strategy/rubric.md && grep -c "Score 1" harnesses/pricing-strategy/rubric.md && echo "PASS: anti-verbosity and all score anchors present"</automated>
  </verify>
  <done>prompt.md exists with data dictionary covering all CSV column names; rubric.md has anti-verbosity instruction and score anchors at 1/3/5 for all three dimensions; completeness dimension references summary, recommendations, and market_observations by name</done>
</task>

</tasks>

<verification>
- `npx tsx -e "import { z } from 'zod'; import { PricingRecommendation } from './harnesses/pricing-strategy/schema.ts'; const s = z.toJSONSchema(PricingRecommendation); console.log(s.type)"` prints "object"
- `ls harnesses/pricing-strategy/` shows: harness.yaml, prompt.md, schema.ts, rubric.md, data/
- `ls harnesses/pricing-strategy/data/` shows: products.csv, sales.csv
- `grep "Do not score higher" harnesses/pricing-strategy/rubric.md` returns a match
- `grep "version: 1.0.0" harnesses/pricing-strategy/harness.yaml` returns a match
</verification>

<success_criteria>
- z.toJSONSchema(PricingRecommendation) succeeds without error
- harness.yaml contains version, data, prompt, output, eval, providers fields
- rubric.md has anti-verbosity instruction + score anchors at 1, 3, 5 for all three dimensions
- No real brand names or unscaled prices in CSVs
- prompt.md data dictionary defines all key columns: current_price, cost_per_unit, units_sold_30d, revenue_30d, price_changes_90d, avg_price_90d
</success_criteria>

<output>
After completion, create `.planning/phases/01-harness-definitions/01-03-SUMMARY.md`
</output>
