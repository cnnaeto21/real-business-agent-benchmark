---
phase: 05-dashboard
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - web/vercel.json
  - docs/running.md
autonomous: false
requirements:
  - DASH-01
  - DASH-05

must_haves:
  truths:
    - "A publicly accessible Vercel URL returns HTTP 200 and renders the dashboard without errors"
    - "docs/running.md exists in the repo and is reachable from the dashboard's 'Run it yourself' link"
    - "The deployed dashboard shows the model comparison table and dimension charts on first load"
  artifacts:
    - path: "web/vercel.json"
      provides: "Vercel framework declaration for the web/ subdirectory"
      contains: '{"framework": "nextjs"}'
    - path: "docs/running.md"
      provides: "Step-by-step local benchmark instructions linked from the dashboard"
  key_links:
    - from: "Vercel project settings"
      to: "web/"
      via: "Root Directory = web set in Vercel dashboard"
      pattern: "vercel.json framework declaration"
    - from: "web/components/RunItYourself.tsx"
      to: "docs/running.md"
      via: "GitHub blob URL link in JSX"
      pattern: "docs/running\\.md"
---

<objective>
Create web/vercel.json, write docs/running.md, deploy the dashboard to Vercel, and verify the public URL is accessible.

Purpose: Makes DASH-01 true (publicly accessible on Vercel) and provides the docs/running.md target that DASH-05's link points to.
Output: Live Vercel deployment URL + committed docs/running.md.
</objective>

<execution_context>
@/Users/obinnaeto/.claude/get-shit-done/workflows/execute-plan.md
@/Users/obinnaeto/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/05-dashboard/05-RESEARCH.md
@.planning/phases/05-dashboard/05-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create web/vercel.json and docs/running.md</name>
  <files>
    web/vercel.json
    docs/running.md
  </files>
  <action>
**Step A — Create web/vercel.json**

```json
{
  "framework": "nextjs"
}
```

This tells Vercel to treat web/ as a Next.js project when Root Directory is set to `web` in the Vercel project settings. No other config is needed.

**Step B — Create docs/running.md**

Create `/Users/obinnaeto/Desktop/agentHarness/docs/running.md`:

```markdown
# Running a Benchmark Locally

This guide walks you through running any RBAB harness against any supported model on your own machine.

## Prerequisites

- Node.js 18 or later
- npm 9 or later
- API key for your chosen provider

## 1. Clone and Install

```bash
git clone https://github.com/obinnaeto/agentHarness.git
cd agentHarness
npm install
```

## 2. Set Environment Variables

Set the API key for the provider you want to use:

```bash
# Anthropic
export ANTHROPIC_API_KEY=your_key_here

# OpenAI
export OPENAI_API_KEY=your_key_here

# Google
export GOOGLE_API_KEY=your_key_here
```

## 3. Run a Benchmark

```bash
benchmark --harness inventory-optimization --model anthropic/claude-sonnet-4-6
```

Available harnesses:
- `inventory-optimization`
- `pricing-strategy`
- `financial-forecasting`

Available models:
- `anthropic/claude-sonnet-4-6`
- `openai/gpt-4o-mini`
- `google/gemini-3.1-flash-lite-preview`

## 4. View Results

Results are written to `results/<run-id>/`:
- `raw/<model-slug>.json` — raw model output
- `scored/<model-slug>.json` — scored result with judge rationale
- `meta.json` — run metadata (cost, latency, tokens)

`results/index.json` is updated after each scored run.

## 5. Re-Score an Existing Run

To apply the judge to a run that already has raw output (e.g., after a rubric update):

```bash
npx ts-node scripts/re-eval.ts --run-id <run-id>
```

## Troubleshooting

**ENOENT on harness files:** Run `benchmark` from the repo root, not a subdirectory.

**Missing API key:** The CLI will exit with a clear error message naming the missing variable.

**Schema validation failure:** The model output did not match the expected JSON schema. The run manifest and raw output are still saved. Check `results/<run-id>/raw/` for the raw response.
```

**Step C — Update the GitHub URL in RunItYourself.tsx**

After you know the actual repo URL, update the `href` in `web/components/RunItYourself.tsx` to point to the correct GitHub blob URL for `docs/running.md`. If the repo is already public at `https://github.com/obinnaeto/agentHarness`, the URL `https://github.com/obinnaeto/agentHarness/blob/main/docs/running.md` is correct — no change needed.
  </action>
  <verify>
    <automated>test -f /Users/obinnaeto/Desktop/agentHarness/web/vercel.json && test -f /Users/obinnaeto/Desktop/agentHarness/docs/running.md && echo "Both files exist"</automated>
  </verify>
  <done>
    - web/vercel.json exists containing `{"framework": "nextjs"}`
    - docs/running.md exists with install, env var, and run instructions
    - cd web && npm run build still exits 0 after these additions
  </done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Deploy to Vercel and configure project settings</name>
  <action>
    HUMAN ACTION REQUIRED. Claude cannot perform this action via CLI or API.

    Steps:
    1. Commit all files if not already committed:
       git add web/ docs/running.md scripts/verify-dashboard-data.ts
       git commit -m "feat(dashboard): add Next.js dashboard, data loader, components"

    2. Push to remote: git push origin main

    3. Go to https://vercel.com/new and import the agentHarness repository.

    4. In the "Configure Project" step:
       - Set Root Directory to "web" (click Edit next to the root directory field)
       - Framework Preset should auto-detect as "Next.js"
       - No environment variables needed (data is read from committed files at build time)

    5. Click Deploy. Wait for the build to complete (~1-2 minutes).

    6. Confirm the deployment URL returns a working dashboard:
       - Model comparison table is visible
       - "Run it yourself" section is visible
       - Bar charts appear (may take a moment to render client-side)

    7. Copy the deployment URL.
  </action>
  <verify>
    <automated>MISSING — verification requires human to open the Vercel deployment URL in a browser and confirm HTTP 200 with rendered dashboard content</automated>
  </verify>
  <done>
    Vercel deployment URL is publicly accessible. Human has confirmed the dashboard renders the model comparison table, dimension charts, and "Run it yourself" section.
  </done>
  <what-built>
    web/vercel.json is committed. The Next.js dashboard is fully built and passes `npm run build` locally. docs/running.md is committed.
  </what-built>
  <how-to-verify>
    1. Commit all files if not already committed:
       ```bash
       git add web/ docs/running.md scripts/verify-dashboard-data.ts
       git commit -m "feat(dashboard): add Next.js dashboard, data loader, components"
       ```

    2. Push to your remote:
       ```bash
       git push origin main
       ```

    3. Go to https://vercel.com/new and import the agentHarness repository.

    4. In the "Configure Project" step:
       - Set **Root Directory** to `web` (click "Edit" next to the root directory field)
       - Framework Preset should auto-detect as "Next.js"
       - No environment variables needed (data is read from committed files at build time)

    5. Click **Deploy**.

    6. Wait for the build to complete (typically 1-2 minutes).

    7. Confirm the deployment URL (e.g. `https://agent-harness-xxx.vercel.app`) returns a working dashboard:
       - Model comparison table is visible
       - "Run it yourself" section is visible
       - Bar charts appear (may take a moment to render client-side)

    8. Copy the deployment URL.
  </how-to-verify>
  <resume-signal>
    Type the public Vercel URL (e.g. "https://agent-harness-xxx.vercel.app") once the deployment is live and confirmed working, or describe any errors encountered.
  </resume-signal>
</task>

<task type="auto">
  <name>Task 3: Fix any Vercel build failures and finalize</name>
  <files>
    web/vercel.json
  </files>
  <action>
After the human checkpoint confirms the Vercel URL:

1. If the Vercel build failed due to path resolution (ENOENT reading results/index.json), it means Vercel's build process cwd differs from expected. In that case:

   - Check build logs for the actual `process.cwd()` value
   - Update `web/lib/data.ts` to use `process.env.RESULTS_PATH ?? path.join(process.cwd(), '..', 'results')` as the base path
   - Add `RESULTS_PATH` to Vercel project settings → Environment Variables if needed
   - Push fix and re-deploy

2. If the build succeeded, no code changes are needed. Run final verification:

   ```bash
   cd /Users/obinnaeto/Desktop/agentHarness/web && npm run build && npm run lint
   ```

3. Record the Vercel deployment URL in the plan summary (vercel.json does not support comments).
  </action>
  <verify>
    <automated>cd /Users/obinnaeto/Desktop/agentHarness/web && npm run build && npm run lint</automated>
  </verify>
  <done>
    - `cd web && npm run build && npm run lint` exits 0
    - Public Vercel URL is live and confirmed by human checkpoint
    - All 5 DASH requirements (DASH-01 through DASH-05) are satisfied
  </done>
</task>

</tasks>

<verification>
Manual verification (after Task 2 checkpoint):
1. Open the Vercel deployment URL in a browser
2. Confirm no error page, no build failure banner
3. Confirm the model comparison table is visible on first load (no interaction required)
4. Confirm per-dimension bar charts appear for each harness
5. Confirm "Run it yourself" section is visible with copy button and docs/running.md link
6. Confirm each table row shows: model name, run date, harness version, temperature

Automated final check:
```bash
cd /Users/obinnaeto/Desktop/agentHarness/web && npm run build && npm run lint
```
</verification>

<success_criteria>
- web/vercel.json committed with `{"framework": "nextjs"}`
- docs/running.md committed with install, env var, and run instructions
- Vercel deployment URL is publicly accessible and returns HTTP 200
- Dashboard renders model comparison table and dimension charts without JavaScript errors
- "Run it yourself" section is equally prominent as the results table (both above the fold or equivalent visual weight)
- All 5 requirement IDs satisfied: DASH-01 (public URL), DASH-02 (table), DASH-03 (charts), DASH-04 (metadata + temperature), DASH-05 (run it yourself section)
</success_criteria>

<output>
After completion, create `.planning/phases/05-dashboard/05-02-SUMMARY.md` using the summary template.
Include the Vercel deployment URL in the summary's key outputs section.
</output>
