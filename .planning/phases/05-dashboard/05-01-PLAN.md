---
phase: 05-dashboard
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/verify-dashboard-data.ts
  - web/package.json
  - web/next.config.ts
  - web/tsconfig.json
  - web/app/globals.css
  - web/app/layout.tsx
  - web/app/page.tsx
  - web/lib/data.ts
  - web/components/ScoresTable.tsx
  - web/components/DimensionChart.tsx
  - web/components/RunItYourself.tsx
  - web/components/CopyButton.tsx
autonomous: true
requirements:
  - DASH-02
  - DASH-03
  - DASH-04
  - DASH-05

must_haves:
  truths:
    - "Running `cd web && npm run build` exits 0 with no TypeScript or lint errors"
    - "The scores table renders all 9 results (3 harnesses x 3 models) with composite score, cost USD, and latency ms"
    - "The dimension chart shows bars for actionability, reasoning_transparency, and completeness for each model"
    - "Each result entry displays model name, run date, harness version, and temperature read from meta.json"
    - "A 'Run it yourself' section appears on the page with a copy-paste CLI command and a docs/running.md link"
    - "All 9 index.json entries and all 9 meta.json files pass the verify-dashboard-data.ts checks"
  artifacts:
    - path: "scripts/verify-dashboard-data.ts"
      provides: "Build-time data integrity verification for all 9 results"
      exports: []
    - path: "web/lib/data.ts"
      provides: "RunResult type + loadResults() function that merges index.json with meta.json temperature"
      exports:
        - RunResult
        - DimensionScore
        - loadResults
    - path: "web/app/page.tsx"
      provides: "Root Server Component — reads results at build time, renders all sections"
    - path: "web/components/ScoresTable.tsx"
      provides: "Model comparison table (Server Component)"
    - path: "web/components/DimensionChart.tsx"
      provides: "Per-dimension bar chart (Client Component, 'use client')"
    - path: "web/components/RunItYourself.tsx"
      provides: "Run it yourself section with CopyButton"
    - path: "web/components/CopyButton.tsx"
      provides: "Clipboard copy button (Client Component, 'use client')"
  key_links:
    - from: "web/app/page.tsx"
      to: "web/lib/data.ts"
      via: "import { loadResults }"
      pattern: "loadResults\\(\\)"
    - from: "web/lib/data.ts"
      to: "results/index.json"
      via: "fs.readFile at path.join(process.cwd(), '..', 'results', 'index.json')"
      pattern: "process\\.cwd\\(\\).*results.*index\\.json"
    - from: "web/lib/data.ts"
      to: "results/<run-id>/meta.json"
      via: "Promise.all merging temperature from each run's meta.json"
      pattern: "meta\\.json"
    - from: "web/app/page.tsx"
      to: "web/components/DimensionChart.tsx"
      via: "dynamic import with ssr: false to avoid hydration mismatch"
      pattern: "dynamic.*DimensionChart.*ssr.*false"
---

<objective>
Scaffold the web/ Next.js dashboard, build all data-loading and UI components, and verify the build passes locally against real results/index.json data.

Purpose: Produces the complete dashboard codebase (data loader, comparison table, dimension chart, "Run it yourself" section) ready for Vercel deployment in Plan 02.
Output: web/ directory with a passing `npm run build`, plus scripts/verify-dashboard-data.ts confirming data integrity.
</objective>

<execution_context>
@/Users/obinnaeto/.claude/get-shit-done/workflows/execute-plan.md
@/Users/obinnaeto/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-dashboard/05-RESEARCH.md

<interfaces>
<!-- Actual index.json shape (confirmed by direct inspection) -->
<!-- 9 entries: 3 harnesses x 3 models -->
```typescript
// Each entry in results/index.json
{
  run_id: string;           // UUID e.g. "7090d572-8a7b-4781-b881-8e4d3e9244e1"
  harness: string;          // "inventory-optimization" | "pricing-strategy" | "financial-forecasting"
  harness_version: string;  // "1.0.0"
  model: string;            // "anthropic/claude-sonnet-4-6" | "openai/gpt-4o-mini" | "google/gemini-3.1-flash-lite-preview"
  composite_score: number;  // 0-100
  schema_valid: boolean;
  scores: {
    actionability: { score: number; rationale: string };         // score 1-5
    reasoning_transparency: { score: number; rationale: string }; // score 1-5
    completeness: { score: number; rationale: string };          // score 1-5
  };
  cost_usd: number;         // e.g. 0.052302
  latency_ms: number;       // e.g. 41050
  run_date: string;         // ISO 8601 e.g. "2026-03-19T23:57:10.029Z"
}

// Each results/<run-id>/meta.json
{
  run_id: string;
  harness: string;
  harness_version: string;
  model: string;
  provider_api_version: string;
  temperature: number;      // e.g. 0
  max_tokens: number;
  input_tokens: number;
  output_tokens: number;
  cost_usd: number;
  latency_ms: number;
  run_date: string;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 0: Create scripts/verify-dashboard-data.ts and scaffold web/ app</name>
  <files>
    scripts/verify-dashboard-data.ts
    web/package.json
    web/next.config.ts
    web/tsconfig.json
    web/app/globals.css
    web/app/layout.tsx
  </files>
  <action>
**Step A — Create scripts/verify-dashboard-data.ts**

Create `/Users/obinnaeto/Desktop/agentHarness/scripts/verify-dashboard-data.ts`:

```typescript
#!/usr/bin/env node
/**
 * verify-dashboard-data.ts
 * Confirms all 9 index.json entries have required fields and
 * all 9 meta.json files exist with a temperature field.
 * Exits 0 on success, 1 on any failure.
 */
import { readFile } from 'fs/promises';
import path from 'path';

const ROOT = path.join(process.cwd());
const REQUIRED_INDEX_FIELDS = [
  'run_id', 'harness', 'model', 'composite_score',
  'scores', 'cost_usd', 'latency_ms', 'run_date',
];

interface IndexEntry {
  run_id: string;
  harness: string;
  model: string;
  composite_score: number;
  scores: { actionability: unknown; reasoning_transparency: unknown; completeness: unknown };
  cost_usd: number;
  latency_ms: number;
  run_date: string;
  [key: string]: unknown;
}

interface MetaJson {
  temperature: number;
  [key: string]: unknown;
}

async function main() {
  let ok = true;

  // 1. Load index.json
  const indexPath = path.join(ROOT, 'results', 'index.json');
  let entries: IndexEntry[];
  try {
    entries = JSON.parse(await readFile(indexPath, 'utf-8')) as IndexEntry[];
  } catch (e) {
    console.error(`FAIL: cannot read ${indexPath}:`, e);
    process.exit(1);
  }

  // 2. Expect exactly 9 entries
  if (entries.length !== 9) {
    console.error(`FAIL: expected 9 entries in index.json, got ${entries.length}`);
    ok = false;
  } else {
    console.log(`OK: index.json has 9 entries`);
  }

  // 3. Check required fields on each entry
  for (const entry of entries) {
    for (const field of REQUIRED_INDEX_FIELDS) {
      if (entry[field] === undefined || entry[field] === null) {
        console.error(`FAIL: entry ${entry.run_id} missing field "${field}"`);
        ok = false;
      }
    }
    // Check scores sub-fields
    for (const dim of ['actionability', 'reasoning_transparency', 'completeness']) {
      const s = entry.scores as Record<string, unknown>;
      if (!s?.[dim]) {
        console.error(`FAIL: entry ${entry.run_id} missing scores.${dim}`);
        ok = false;
      }
    }
  }
  if (ok) console.log(`OK: all index.json entries have required fields`);

  // 4. Check each meta.json exists and has temperature
  for (const entry of entries) {
    const metaPath = path.join(ROOT, 'results', entry.run_id, 'meta.json');
    let meta: MetaJson;
    try {
      meta = JSON.parse(await readFile(metaPath, 'utf-8')) as MetaJson;
    } catch {
      console.error(`FAIL: cannot read meta.json for run ${entry.run_id}`);
      ok = false;
      continue;
    }
    if (typeof meta.temperature !== 'number') {
      console.error(`FAIL: meta.json for ${entry.run_id} missing temperature field`);
      ok = false;
    } else {
      console.log(`OK: meta.json for ${entry.run_id} has temperature=${meta.temperature}`);
    }
  }

  if (!ok) {
    console.error('\nverify-dashboard-data: FAILED');
    process.exit(1);
  }
  console.log('\nverify-dashboard-data: ALL CHECKS PASSED');
}

main();
```

**Step B — Scaffold web/ app**

Run from the repo root (not inside web/):
```bash
npx create-next-app@latest web --ts --tailwind --eslint --app --no-src-dir --yes
```

This creates web/ with App Router, TypeScript, Tailwind v4, and ESLint.

**Step C — Install recharts**

```bash
cd /Users/obinnaeto/Desktop/agentHarness/web && npm install recharts
```

**Step D — Update web/next.config.ts** (replace whatever create-next-app generated):

```typescript
import type { NextConfig } from 'next';

const nextConfig: NextConfig = {
  // No output: 'export' — Vercel handles static generation natively
};

export default nextConfig;
```

**Step E — Confirm web/app/globals.css has Tailwind v4 import** (create-next-app --tailwind should set this, but verify):

The file should contain `@import "tailwindcss";` at the top. Do not create a `tailwind.config.js`.

**Step F — Replace web/app/layout.tsx** with minimal layout (keep what create-next-app generated if it imports globals.css correctly; ensure title is "RBAB Dashboard"):

```typescript
import type { Metadata } from 'next';
import './globals.css';

export const metadata: Metadata = {
  title: 'RBAB Dashboard',
  description: 'Real Business Agent Benchmark — model comparison results',
};

export default function RootLayout({ children }: { children: React.ReactNode }) {
  return (
    <html lang="en">
      <body className="bg-gray-50 text-gray-900 min-h-screen">
        {children}
      </body>
    </html>
  );
}
```
  </action>
  <verify>
    <automated>node /Users/obinnaeto/Desktop/agentHarness/scripts/verify-dashboard-data.ts</automated>
  </verify>
  <done>
    - scripts/verify-dashboard-data.ts exits 0 confirming 9 index.json entries and 9 meta.json files with temperature
    - web/ directory exists with package.json containing "recharts" as a dependency
    - web/next.config.ts exists with no `output: 'export'`
  </done>
</task>

<task type="auto">
  <name>Task 1: Build data loader, all components, and page.tsx</name>
  <files>
    web/lib/data.ts
    web/components/ScoresTable.tsx
    web/components/DimensionChart.tsx
    web/components/RunItYourself.tsx
    web/components/CopyButton.tsx
    web/app/page.tsx
  </files>
  <action>
**Step A — Create web/lib/data.ts**

```typescript
import { readFile } from 'fs/promises';
import path from 'path';

export interface DimensionScore {
  score: number;    // 1-5
  rationale: string;
}

export interface RunResult {
  run_id: string;
  harness: 'inventory-optimization' | 'pricing-strategy' | 'financial-forecasting';
  harness_version: string;
  model: string;
  composite_score: number;
  schema_valid: boolean;
  scores: {
    actionability: DimensionScore;
    reasoning_transparency: DimensionScore;
    completeness: DimensionScore;
  };
  cost_usd: number;
  latency_ms: number;
  run_date: string;
  // merged from meta.json
  temperature: number;
}

export async function loadResults(): Promise<RunResult[]> {
  // process.cwd() = web/ when running next build from web/ (both locally and on Vercel)
  const indexPath = path.join(process.cwd(), '..', 'results', 'index.json');
  const raw = JSON.parse(await readFile(indexPath, 'utf-8')) as Omit<RunResult, 'temperature'>[];

  return Promise.all(
    raw.map(async (entry) => {
      const metaPath = path.join(
        process.cwd(), '..', 'results', entry.run_id, 'meta.json'
      );
      const meta = JSON.parse(await readFile(metaPath, 'utf-8')) as { temperature: number };
      return { ...entry, temperature: meta.temperature };
    })
  );
}

/** Strip provider prefix from model string: "anthropic/claude-sonnet-4-6" -> "claude-sonnet-4-6" */
export function modelLabel(model: string): string {
  return model.includes('/') ? model.split('/')[1] : model;
}

/** Format harness name for display */
export function harnessLabel(harness: string): string {
  return harness.split('-').map(w => w[0].toUpperCase() + w.slice(1)).join(' ');
}
```

**Step B — Create web/components/ScoresTable.tsx** (Server Component — no 'use client'):

```typescript
import type { RunResult } from '@/lib/data';
import { modelLabel, harnessLabel } from '@/lib/data';

interface Props {
  results: RunResult[];
}

const HARNESSES = [
  'inventory-optimization',
  'pricing-strategy',
  'financial-forecasting',
] as const;

export default function ScoresTable({ results }: Props) {
  // Get unique model labels
  const models = [...new Set(results.map(r => r.model))];

  return (
    <section className="mb-12">
      <h2 className="text-2xl font-bold mb-4">Model Comparison</h2>
      <div className="overflow-x-auto">
        <table className="w-full border-collapse bg-white shadow rounded-lg">
          <thead>
            <tr className="bg-gray-100 text-left">
              <th className="px-4 py-3 font-semibold text-gray-700">Model</th>
              <th className="px-4 py-3 font-semibold text-gray-700">Harness</th>
              <th className="px-4 py-3 font-semibold text-gray-700 text-right">Score</th>
              <th className="px-4 py-3 font-semibold text-gray-700 text-right">Cost (USD)</th>
              <th className="px-4 py-3 font-semibold text-gray-700 text-right">Latency (ms)</th>
              <th className="px-4 py-3 font-semibold text-gray-700">Run Date</th>
              <th className="px-4 py-3 font-semibold text-gray-700">Harness Ver.</th>
              <th className="px-4 py-3 font-semibold text-gray-700 text-right">Temp.</th>
            </tr>
          </thead>
          <tbody>
            {models.map(model =>
              HARNESSES.map(harness => {
                const r = results.find(x => x.model === model && x.harness === harness);
                if (!r) return null;
                return (
                  <tr key={r.run_id} className="border-t border-gray-200 hover:bg-gray-50">
                    <td className="px-4 py-3 font-mono text-sm">{modelLabel(model)}</td>
                    <td className="px-4 py-3 text-sm">{harnessLabel(harness)}</td>
                    <td className="px-4 py-3 text-right font-bold">{r.composite_score}</td>
                    <td className="px-4 py-3 text-right font-mono text-sm">${r.cost_usd.toFixed(5)}</td>
                    <td className="px-4 py-3 text-right font-mono text-sm">{r.latency_ms.toLocaleString()}</td>
                    <td className="px-4 py-3 text-sm">{new Date(r.run_date).toLocaleDateString()}</td>
                    <td className="px-4 py-3 text-sm font-mono">{r.harness_version}</td>
                    <td className="px-4 py-3 text-right font-mono text-sm">{r.temperature}</td>
                  </tr>
                );
              })
            )}
          </tbody>
        </table>
      </div>
    </section>
  );
}
```

**Step C — Create web/components/DimensionChart.tsx** (Client Component — must have 'use client'):

```typescript
'use client';

import {
  BarChart, Bar, XAxis, YAxis, CartesianGrid,
  Tooltip, Legend, ResponsiveContainer,
} from 'recharts';
import type { RunResult } from '@/lib/data';
import { modelLabel, harnessLabel } from '@/lib/data';

interface Props {
  results: RunResult[];
}

const HARNESSES = [
  'inventory-optimization',
  'pricing-strategy',
  'financial-forecasting',
] as const;

export default function DimensionChart({ results }: Props) {
  return (
    <section className="mb-12">
      <h2 className="text-2xl font-bold mb-4">Per-Dimension Scores</h2>
      <p className="text-sm text-gray-600 mb-6">Scores 1–5 per dimension per model, grouped by harness.</p>
      {HARNESSES.map(harness => {
        const harnesResults = results.filter(r => r.harness === harness);
        const chartData = harnesResults.map(r => ({
          model: modelLabel(r.model),
          actionability: r.scores.actionability.score,
          reasoning_transparency: r.scores.reasoning_transparency.score,
          completeness: r.scores.completeness.score,
        }));

        return (
          <div key={harness} className="mb-8">
            <h3 className="text-lg font-semibold mb-3">{harnessLabel(harness)}</h3>
            <div style={{ width: '100%', height: 280 }}>
              <ResponsiveContainer width="100%" height="100%">
                <BarChart data={chartData} margin={{ top: 5, right: 20, left: 0, bottom: 5 }}>
                  <CartesianGrid strokeDasharray="3 3" />
                  <XAxis dataKey="model" />
                  <YAxis domain={[0, 5]} ticks={[1, 2, 3, 4, 5]} />
                  <Tooltip />
                  <Legend />
                  <Bar dataKey="actionability" fill="#6366f1" />
                  <Bar dataKey="reasoning_transparency" fill="#8b5cf6" name="reasoning transparency" />
                  <Bar dataKey="completeness" fill="#a78bfa" />
                </BarChart>
              </ResponsiveContainer>
            </div>
          </div>
        );
      })}
    </section>
  );
}
```

**Step D — Create web/components/CopyButton.tsx** (Client Component):

```typescript
'use client';

import { useState } from 'react';

interface Props {
  text: string;
}

export default function CopyButton({ text }: Props) {
  const [copied, setCopied] = useState(false);

  async function handleCopy() {
    await navigator.clipboard.writeText(text);
    setCopied(true);
    setTimeout(() => setCopied(false), 2000);
  }

  return (
    <button
      onClick={handleCopy}
      className="ml-3 px-3 py-1 text-sm bg-indigo-100 hover:bg-indigo-200 text-indigo-700 rounded transition-colors"
    >
      {copied ? 'Copied!' : 'Copy'}
    </button>
  );
}
```

**Step E — Create web/components/RunItYourself.tsx** (Server Component):

```typescript
import CopyButton from './CopyButton';

const COMMAND = 'benchmark --harness inventory-optimization --model anthropic/claude-sonnet-4-6';

export default function RunItYourself() {
  return (
    <section className="mb-12 p-6 bg-white shadow rounded-lg border-l-4 border-indigo-500">
      <h2 className="text-2xl font-bold mb-3">Run it yourself</h2>
      <p className="text-gray-600 mb-4">
        Reproduce any of these results locally in minutes. Requires API keys for your chosen provider.
      </p>
      <div className="bg-gray-900 text-green-300 rounded-lg px-4 py-3 flex items-center justify-between font-mono text-sm mb-4">
        <code>{COMMAND}</code>
        <CopyButton text={COMMAND} />
      </div>
      <a
        href="https://github.com/obinnaeto/agentHarness/blob/main/docs/running.md"
        className="text-indigo-600 hover:text-indigo-800 underline font-medium"
        target="_blank"
        rel="noopener noreferrer"
      >
        Full setup instructions in docs/running.md →
      </a>
    </section>
  );
}
```

**Step F — Create web/app/page.tsx** (Server Component — no 'use client'):

Use `dynamic` import for DimensionChart with `ssr: false` to avoid hydration mismatch from ResponsiveContainer.

```typescript
import dynamic from 'next/dynamic';
import { loadResults } from '@/lib/data';
import ScoresTable from '@/components/ScoresTable';
import RunItYourself from '@/components/RunItYourself';

// Recharts uses browser-only APIs — disable SSR to avoid hydration mismatch
const DimensionChart = dynamic(() => import('@/components/DimensionChart'), { ssr: false });

export default async function DashboardPage() {
  const results = await loadResults();

  return (
    <main className="max-w-6xl mx-auto px-4 py-10">
      <header className="mb-10">
        <h1 className="text-4xl font-bold mb-2">Real Business Agent Benchmark</h1>
        <p className="text-gray-600 text-lg">
          9 reference runs across 3 harnesses and 3 models — scored on actionability,
          reasoning transparency, and completeness.
        </p>
      </header>

      <RunItYourself />
      <ScoresTable results={results} />
      <DimensionChart results={results} />
    </main>
  );
}
```
  </action>
  <verify>
    <automated>cd /Users/obinnaeto/Desktop/agentHarness/web && npm run build</automated>
  </verify>
  <done>
    - `cd web && npm run build` exits 0 with no errors
    - All 6 files exist: web/lib/data.ts, web/components/ScoresTable.tsx, web/components/DimensionChart.tsx, web/components/CopyButton.tsx, web/components/RunItYourself.tsx, web/app/page.tsx
    - DimensionChart is loaded via dynamic import with ssr: false
    - loadResults() merges temperature from meta.json for all 9 runs
  </done>
</task>

</tasks>

<verification>
Full build and lint pass from web/ directory:
```bash
cd /Users/obinnaeto/Desktop/agentHarness/web && npm run build && npm run lint
```

Data integrity check:
```bash
node /Users/obinnaeto/Desktop/agentHarness/scripts/verify-dashboard-data.ts
```

Both commands must exit 0 before this plan is considered complete.
</verification>

<success_criteria>
- scripts/verify-dashboard-data.ts exits 0 (9 entries, 9 meta.json files with temperature)
- `cd web && npm run build` exits 0 (no TypeScript errors, no missing modules)
- `cd web && npm run lint` exits 0 (no ESLint errors)
- web/lib/data.ts exports RunResult type with temperature field
- web/components/DimensionChart.tsx has 'use client' directive
- web/app/page.tsx uses dynamic() with ssr: false for DimensionChart
- Dashboard would show all DASH-02, DASH-03, DASH-04, DASH-05 content if opened in a browser
</success_criteria>

<output>
After completion, create `.planning/phases/05-dashboard/05-01-SUMMARY.md` using the summary template.
</output>
