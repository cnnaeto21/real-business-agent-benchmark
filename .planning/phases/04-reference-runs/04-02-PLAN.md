---
phase: 04-reference-runs
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - results/index.json
  - results/**/meta.json
  - results/**/raw/*.json
  - results/**/scored/*.json
  - src/cost.ts
autonomous: false
requirements:
  - REF-01
  - REF-02
  - REF-03

must_haves:
  truths:
    - "Nine result directories exist under results/ — one per harness×model combination"
    - "Every results/<run-id>/scored/<model-slug>.json has schema_valid: true"
    - "results/index.json contains exactly 9 entries covering all 3 harnesses × 3 models"
    - "Every meta.json contains: model, provider_api_version, temperature, max_tokens, input_tokens, output_tokens, cost_usd, latency_ms, run_date"
    - "docs/judge-prompt.md exists in the repo (committed in Phase 1)"
    - "All 9 result directories and the final index.json are committed to git in a single atomic commit"
  artifacts:
    - path: "results/index.json"
      provides: "Flat array of 9 IndexEntry objects — consumed by Phase 5 dashboard"
      contains: "9 entries, all schema_valid: true"
    - path: "results/<run-id>/meta.json"
      provides: "Run manifest per result"
      contains: "model, provider_api_version, temperature, max_tokens, input_tokens, output_tokens, cost_usd, latency_ms"
    - path: "results/<run-id>/scored/<model-slug>.json"
      provides: "Scored result with composite score and per-dimension rationales"
      contains: "composite_score, schema_valid: true, scores"
  key_links:
    - from: "results/index.json"
      to: "Phase 5 dashboard"
      via: "consumed at Next.js build time"
      pattern: "9 entries"
    - from: "scripts/run-reference.ts"
      to: "results/"
      via: "benchmark CLI subprocess writes all artifacts"
      pattern: "npm run reference"
---

<objective>
Execute the nine reference benchmark runs using `npm run reference`, verify all 9 results pass the quality gate, and commit them to git in a single atomic commit.

This plan is the DATA deliverable of Phase 4. Plan 01 built the orchestration scripts; this plan uses them to produce the committed reference results that Phase 5 (dashboard) depends on.

Purpose: Populate `results/` with 9 scored benchmark runs (3 harnesses × 3 models) and commit them as the public baseline.
Output: 9 result directories, populated `results/index.json`, single git commit containing all reference data.
</objective>

<execution_context>
@/Users/obinnaeto/.claude/get-shit-done/workflows/execute-plan.md
@/Users/obinnaeto/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-reference-runs/04-CONTEXT.md
@.planning/phases/04-reference-runs/04-RESEARCH.md
@.planning/phases/04-reference-runs/04-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-flight — verify env vars, re-verify cost table, dry-run harness validation</name>
  <files>src/cost.ts</files>
  <action>
Before spending API credits, perform pre-flight checks and cost table verification.

**Step 1: Run dry-run to confirm harness loading and env var presence**
```bash
npm run reference -- --dry-run
```
All 3 harnesses must show PASS. All 3 env vars (ANTHROPIC_API_KEY, OPENAI_API_KEY, GOOGLE_API_KEY) must show PASS. If any env var is missing, stop — executor cannot proceed without credentials.

**Step 2: Re-verify cost table in src/cost.ts**
The RESEARCH.md notes: "Re-verify prices before committing reference runs in Phase 4. Prices are correct as of 2026-03-15 but may change."

Check current official pricing for:
- Anthropic Claude Sonnet: https://www.anthropic.com/pricing (or API docs)
- OpenAI GPT-4o: https://openai.com/api/pricing
- Google Gemini 1.5 Pro: https://ai.google.dev/pricing

Current values in `src/cost.ts` PRICE_TABLE (from RESEARCH.md):
- `anthropic/claude-sonnet-4-6`: inputPerMTok: 3.00, outputPerMTok: 15.00
- `openai/gpt-4o`: inputPerMTok: 2.50, outputPerMTok: 10.00
- `google/gemini-1.5-pro`: inputPerMTok: 1.25, outputPerMTok: 5.00

If prices have changed: update `src/cost.ts` PRICE_TABLE with correct values and add a comment noting the re-verification date (2026-03-17). If prices are unchanged: leave the file unmodified (no unnecessary git diff).

**Step 3: Verify docs/judge-prompt.md exists**
```bash
ls docs/judge-prompt.md
```
Must exist (committed in Phase 1). If missing, stop — this is a REF-03 blocker.
  </action>
  <verify>
    <automated>cd /Users/obinnaeto/Desktop/agentHarness && npx tsx scripts/run-reference.ts --dry-run && ls docs/judge-prompt.md</automated>
  </verify>
  <done>
- Dry-run exits 0 with all 3 harnesses PASS and all 3 env vars PASS
- `docs/judge-prompt.md` exists
- Cost table re-verified against official pricing pages (updated if changed, unchanged if current)
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Run the 9 reference benchmark calls (~7-10 min, ~$0.55 USD)</name>
  <what-built>
Pre-flight passed: all 3 harnesses load, all 3 provider env vars are set, docs/judge-prompt.md exists, cost table verified. The orchestration script is ready to run 9 sequential benchmark calls costing approximately $0.55 USD total.
  </what-built>
  <how-to-verify>
1. Review the dry-run output above — confirm all items show PASS
2. Confirm you have active API keys for all three providers (Anthropic, OpenAI, Google)
3. Run the reference suite (this will take approximately 7-10 minutes and cost ~$0.55 USD):

```bash
npm run reference
```

4. When prompted `Proceed? (y/N)`, type `y` and press Enter
5. Watch the 9 runs execute sequentially — each should print progress and then show the eval scoring
6. After all runs complete, review the summary table printed at the end:
   - All 9 rows should show status OK or RETRY (FAILED = stop, investigate before committing)
   - composite_score values should be in range 20-100 (any score is valid benchmark data)
   - cost_usd values should be non-zero
7. Confirm the final output says "All 9 reference runs verified"

If any run shows FAILED (after retry): type "failed: [harness] x [model]" and describe the error.
If all 9 pass verification: type "approved"
  </how-to-verify>
  <resume-signal>Type "approved" if all 9 runs completed and verified. Otherwise describe what failed.</resume-signal>
  <action>Run `npm run reference` interactively. The script handles everything: clear results, cost estimate, confirmation prompt, 9 sequential benchmark subprocesses, retry on failure, and final verification. Human confirms results look correct before proceeding to commit.</action>
  <verify>
    <automated>cd /Users/obinnaeto/Desktop/agentHarness && npx tsx scripts/verify-reference.ts</automated>
  </verify>
  <done>All 9 runs completed, summary table shows no FAILED status rows, `npm run verify-reference` exits 0.</done>
</task>

<task type="auto">
  <name>Task 2: Commit reference results atomically</name>
  <files>results/index.json</files>
  <action>
After human confirmation that all 9 runs passed verification, commit all reference results in a single atomic commit.

**Step 1: Final verification pass**
```bash
npm run verify-reference
```
Must exit 0. If it exits 1, do NOT commit — investigate and fix before proceeding.

**Step 2: Stage all reference run artifacts**
```bash
git add results/
git add docs/judge-prompt.md
```
(docs/judge-prompt.md is already committed from Phase 1 — staging it here ensures it appears in the diff for traceability, but it will show as "already up to date" if unchanged.)

**Step 3: Verify what is staged**
```bash
git status
git diff --cached --stat
```
Expected: 9 result directories under results/ (each with meta.json, raw/*.json, scored/*.json), updated results/index.json. Confirm no unrelated files are staged.

**Step 4: Commit**
```bash
git commit -m "$(cat <<'EOF'
data(04-reference-runs): add 9 reference run results

3 harnesses x 3 models (Claude Sonnet, GPT-4o, Gemini 1.5 Pro).
All 9 runs have schema_valid: true. results/index.json populated
with 9 entries for Phase 5 dashboard consumption.

Co-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>
EOF
)"
```

**Step 5: Verify commit**
```bash
git log --oneline -1
git show --stat HEAD
```
Confirm the commit shows all 9 result directories.
  </action>
  <verify>
    <automated>cd /Users/obinnaeto/Desktop/agentHarness && npm run verify-reference && git log --oneline -1</automated>
  </verify>
  <done>
- `npm run verify-reference` exits 0 (9 entries, all schema_valid: true, all 9 harness×model combos present)
- `git log --oneline -1` shows the data(04-reference-runs) commit
- `git show --stat HEAD` shows 9 result directories committed
- `results/index.json` has exactly 9 entries (no test runs from prior phases)
  </done>
</task>

</tasks>

<verification>
```bash
# Full verification suite
cd /Users/obinnaeto/Desktop/agentHarness && npm run verify-reference

# Check committed file count
git show --stat HEAD | grep "results/"

# Spot-check one scored JSON
cat results/$(ls results/ | grep -v index.json | head -1)/scored/*.json | python3 -m json.tool | grep schema_valid
```

Expected: `npm run verify-reference` exits 0; 9 result directories in HEAD commit; spot-checked scored JSON shows `"schema_valid": true`.
</verification>

<success_criteria>
- Nine result directories exist under `results/`, one per harness×model combination (REF-01)
- Every `meta.json` contains all required manifest fields: model, provider_api_version, temperature, max_tokens, input_tokens, output_tokens, cost_usd, latency_ms, run_date (REF-02)
- `docs/judge-prompt.md` is published in the repo (REF-03)
- `results/index.json` has exactly 9 entries, all with `schema_valid: true`
- All 9 result directories are committed in a single atomic git commit
- `npm run verify-reference` exits 0 on the committed state
</success_criteria>

<output>
After completion, create `.planning/phases/04-reference-runs/04-02-SUMMARY.md`
</output>
