---
phase: 06-documentation-and-launch
plan: 02
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - README.md
autonomous: false
requirements:
  - DOCS-01

must_haves:
  truths:
    - "README.md opens above the fold with a tagline, 2-3 sentences on the gap RBAB fills, and the benchmark command — all visible without scrolling"
    - "README.md includes a results table summarizing all 9 reference runs (3 models × 3 harnesses) with a link to the live dashboard for full detail"
    - "README.md has a 3-4 step pipeline summary (How it works), an 'Add your own model' section showing provider prefix patterns, and a Limitations callout linking to LIMITATIONS.md"
    - "README.md credits Bret Taylor's inspiration with a link to the original post"
    - "README.md links to docs/scoring.md, docs/harness-spec.md, docs/running.md, and LIMITATIONS.md — all of which exist after Plan 01"
    - "Following only the README from a clean clone, a user can run npm install && benchmark --harness inventory-optimization --model anthropic/claude-sonnet-4-6 and get a scored result"
  artifacts:
    - path: "README.md"
      provides: "Primary entry point for AI builders and HN audience"
      contains: "benchmark --harness inventory-optimization --model anthropic/claude-sonnet-4-6"
  key_links:
    - from: "README.md"
      to: "LIMITATIONS.md"
      via: "markdown link"
      pattern: "\\[.*\\].*LIMITATIONS\\.md"
    - from: "README.md"
      to: "docs/scoring.md"
      via: "markdown link"
      pattern: "\\[.*\\].*docs/scoring\\.md"
    - from: "README.md"
      to: "[DASHBOARD_URL]"
      via: "markdown link"
      pattern: "http"
---

<objective>
Write README.md — the primary entry point for AI builders discovering RBAB. Depends on Plan 01 for all link targets (docs/scoring.md, docs/harness-spec.md, LIMITATIONS.md). Ends with a human checkpoint to verify the live dashboard URL resolves and the clean-clone flow is correct.

Purpose: Satisfies DOCS-01. Makes the repo self-contained and launch-ready — a new person clones it, reads the README, and runs a benchmark in under 5 minutes.
Output: README.md
</objective>

<execution_context>
@/Users/obinnaeto/.claude/get-shit-done/workflows/execute-plan.md
@/Users/obinnaeto/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-documentation-and-launch/06-CONTEXT.md
@.planning/phases/06-documentation-and-launch/06-RESEARCH.md
@.planning/phases/06-documentation-and-launch/06-01-SUMMARY.md

<interfaces>
<!-- All facts the executor needs to write README.md without exploring the codebase. -->

Reference results table (all 9 runs from results/index.json):
| Model | inventory-optimization | pricing-strategy | financial-forecasting |
|-------|----------------------|------------------|-----------------------|
| anthropic/claude-sonnet-4-6 | 100 | 100 | 100 |
| openai/gpt-4o-mini | 67 | 67 | 60 |
| google/gemini-3.1-flash-lite-preview | 93 | 80 | 87 |

Provider prefix convention (for "Add your own model" section):
- anthropic/<model-id> — uses Anthropic SDK, tool-use structured output
- openai/<model-id> — uses OpenAI SDK, json_schema mode
- google/<model-id> — uses Google GenAI SDK, responseJsonSchema

Install and benchmark commands:
```bash
npm install
benchmark --harness inventory-optimization --model anthropic/claude-sonnet-4-6
```

GitHub repo URL (already in docs/running.md):
https://github.com/obinnaeto/agentHarness.git

URL placeholders (executor fills in during authoring):
- [DASHBOARD_URL] — live Vercel dashboard URL from Phase 5 deployment; executor must look up or ask user
- [BRET_TAYLOR_POST_URL] — URL of Bret Taylor's original post; executor must research and fill in

Pipeline steps (for "How it works" section):
1. Harness — structured task package: CSV data, prompt template, output schema, eval rubric
2. Prompt — CSV data injected into template and sent to the model with structured output enforced
3. Model — any supported provider (Anthropic, OpenAI, Google) returns a schema-conforming JSON response
4. Eval — JSON schema validation gate, then LLM-as-judge scores on 3 dimensions
5. Score — composite score (0-100) written to results/ alongside raw output and run manifest

Required README sections (from 06-CONTEXT.md locked decisions):
1. Above the fold: tagline + 2-3 sentences on the gap filled + benchmark command block
2. Results summary table (9 runs) + link to live dashboard
3. How it works: 4-5 step pipeline
4. Add your own model: short section showing provider prefix pattern and command
5. Limitations callout: 2-3 sentences + link to LIMITATIONS.md
6. Credit: Bret Taylor's inspiration with link to [BRET_TAYLOR_POST_URL]
7. Links to docs: docs/running.md, docs/scoring.md, docs/harness-spec.md

CRITICAL — benchmark command must appear exactly as:
benchmark --harness inventory-optimization --model anthropic/claude-sonnet-4-6

Do NOT use npx prefix or npm run prefix. The bin is installed by npm install to node_modules/.bin.
Note in quick-start: ANTHROPIC_API_KEY must be set for this command. Full env var setup is in docs/running.md.

Do NOT duplicate env var setup, install detail, or result directory layout from docs/running.md. Link there instead.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write README.md</name>
  <files>README.md</files>
  <action>
Create README.md following the structure from 06-CONTEXT.md locked decisions. The audience is AI builders and the HN community — not contributors or general technical readers.

**Section order:**

1. **Above the fold** (no heading needed — this IS the landing):
   - Tagline (one bold line: what RBAB is in 10 words or fewer)
   - 2-3 sentences on the gap: existing benchmarks (MMLU, HumanEval, coding benchmarks) don't test whether agents can actually help run a business; RBAB does, using real operational tasks (inventory restocking, pricing decisions, financial forecasting) with structured output schemas and an LLM judge
   - Single benchmark command in a code block (```bash):
     ```bash
     benchmark --harness inventory-optimization --model anthropic/claude-sonnet-4-6
     ```
   - Note: requires ANTHROPIC_API_KEY set; see docs/running.md for full setup

2. **Results** heading:
   - Insert the 9-run results table from the interfaces block (GitHub Markdown table)
   - Add sentence: "Full detail and per-dimension breakdowns at [DASHBOARD_URL]"

3. **How it works** heading:
   - Numbered list of 4-5 pipeline steps from the interfaces block
   - Link "scoring" to docs/scoring.md

4. **Add your own model** heading (short — 3-5 lines):
   - Show the command pattern with a different model
   - List the 3 provider prefixes: anthropic/, openai/, google/
   - Link to docs/running.md for available models and env var setup

5. **Limitations** heading (brief callout — 2-3 sentences):
   - Honest summary of what RBAB does and doesn't test
   - Link to LIMITATIONS.md for full detail

6. **Credit** heading (1-3 lines):
   - Credit Bret Taylor's inspiration: "RBAB is the implementation of [Bret Taylor's concept]([BRET_TAYLOR_POST_URL]) of a CSS Zen Garden for agent harnesses — a fixed problem set that reveals how different models actually perform on the same task."

7. **Further reading** or **Docs** section (optional, at discretion):
   - Links to docs/running.md, docs/scoring.md, docs/harness-spec.md

Executor must fill in the two URL placeholders:
- [DASHBOARD_URL]: Find the Vercel deployment URL from Phase 5. Check vercel.json in web/ or run `vercel ls` if authenticated. If not findable, use the placeholder text `[paste Vercel dashboard URL here]` and note it in the summary.
- [BRET_TAYLOR_POST_URL]: Search for Bret Taylor's LinkedIn post about a "CSS Zen Garden for AI agents" or similar. If not findable with certainty, use `[paste Bret Taylor post URL here]` and note it in the summary.

Discretion areas (CONTEXT.md): exact tagline wording, section heading text, whether to include GitHub badges, table of contents choice.
  </action>
  <verify>
    <automated>ls /Users/obinnaeto/Desktop/agentHarness/README.md && grep -l "benchmark --harness inventory-optimization" /Users/obinnaeto/Desktop/agentHarness/README.md && grep -l "LIMITATIONS.md" /Users/obinnaeto/Desktop/agentHarness/README.md && grep -l "docs/scoring.md" /Users/obinnaeto/Desktop/agentHarness/README.md</automated>
  </verify>
  <done>README.md exists with the exact benchmark command, links to LIMITATIONS.md and docs/scoring.md, and the 9-run results table. Both URL placeholders either resolved or documented as remaining.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verify — all docs complete and links resolve</name>
  <what-built>
README.md is written. docs/scoring.md, docs/harness-spec.md, and LIMITATIONS.md all exist from Plan 01. The repo now has all documentation for launch.
  </what-built>
  <how-to-verify>
1. Open README.md — confirm the benchmark command appears above the fold and uses exactly: `benchmark --harness inventory-optimization --model anthropic/claude-sonnet-4-6` (no npx, no npm run prefix)
2. Confirm the results table shows all 9 runs (3 models × 3 harnesses) with correct scores from results/index.json
3. Click the dashboard URL — confirm it loads without error
4. Click the Bret Taylor link — confirm it resolves to his original post
5. Open LIMITATIONS.md — count the limitations: should be exactly 4, each with "What this means for you"
6. Open docs/harness-spec.md — search for "User Message Template" — it should appear in the prompt.md section
7. Open docs/scoring.md — confirm the composite formula is present and the representative scored JSON example is embedded
8. Confirm docs/running.md exists and was NOT modified (it was already complete before this phase)
9. Phase success criteria check: could you follow only the README from a clean clone and run the benchmark command?
  </how-to-verify>
  <action>Human verification checkpoint — no automated action. See how-to-verify for manual steps.</action>
  <verify><automated>ls /Users/obinnaeto/Desktop/agentHarness/README.md</automated></verify>
  <done>User has reviewed all documentation and confirmed: benchmark command is correct, dashboard URL resolves, all 4 limitations present, separator requirement documented in harness-spec.md.</done>
  <resume-signal>Type "approved" if all checks pass, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
Full phase verification after checkpoint approval:

```bash
# All 5 required doc files exist
ls /Users/obinnaeto/Desktop/agentHarness/README.md \
   /Users/obinnaeto/Desktop/agentHarness/LIMITATIONS.md \
   /Users/obinnaeto/Desktop/agentHarness/docs/scoring.md \
   /Users/obinnaeto/Desktop/agentHarness/docs/harness-spec.md \
   /Users/obinnaeto/Desktop/agentHarness/docs/running.md \
   /Users/obinnaeto/Desktop/agentHarness/docs/judge-prompt.md

# README has exact benchmark command
grep "benchmark --harness inventory-optimization --model anthropic/claude-sonnet-4-6" /Users/obinnaeto/Desktop/agentHarness/README.md

# LIMITATIONS.md has exactly 4 limitations
grep -c "What this means for you" /Users/obinnaeto/Desktop/agentHarness/LIMITATIONS.md
# expected: 4

# harness-spec.md has separator requirement documented
grep "User Message Template" /Users/obinnaeto/Desktop/agentHarness/docs/harness-spec.md

# scoring.md has composite formula
grep "composite_score" /Users/obinnaeto/Desktop/agentHarness/docs/scoring.md
```
</verification>

<success_criteria>
Phase 6 is complete when all of the following are TRUE:
1. Following only README.md from a clean clone, a user can run `npm install && benchmark --harness inventory-optimization --model anthropic/claude-sonnet-4-6` and get a scored result
2. LIMITATIONS.md explicitly states exactly 4 limitations: single-domain scope, no ground truth, single-pass judge, training data leakage risk — each with a practical implication
3. docs/harness-spec.md documents all required harness files and harness.yaml fields; the # User Message Template separator requirement and {{placeholder}} resolution are explicitly documented
4. docs/scoring.md explains the hybrid scoring system (JSON validation gate + LLM judge), all 3 rubric dimensions with verbatim score anchors, the composite formula, and a representative scored example
5. docs/running.md exists and was not modified (DOCS-03 was already satisfied before this phase)
</success_criteria>

<output>
After completion, create `.planning/phases/06-documentation-and-launch/06-02-SUMMARY.md`
</output>
