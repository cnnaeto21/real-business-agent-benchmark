---
phase: 06-documentation-and-launch
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/scoring.md
  - docs/harness-spec.md
  - LIMITATIONS.md
autonomous: true
requirements:
  - DOCS-02
  - DOCS-03
  - DOCS-04
  - DOCS-05

must_haves:
  truths:
    - "docs/scoring.md explains the hybrid scoring pipeline (schema gate + LLM judge), all three rubric dimensions with verbatim score anchors (1, 3, 5), the composite formula, and includes a real scored JSON example"
    - "docs/harness-spec.md documents every required file in a harness package and every harness.yaml field, with the # User Message Template separator requirement and {{placeholder}} resolution explicitly called out"
    - "LIMITATIONS.md states exactly 4 limitations — single-domain scope, no ground truth, single-pass judge, training data leakage risk — each with a 'what this means for you' implication; no additional limitations added"
    - "DOCS-03 is satisfied: docs/running.md already exists and is complete — no work needed"
  artifacts:
    - path: "docs/scoring.md"
      provides: "Hybrid scoring system documentation"
      contains: "composite_score = (actionability + reasoning_transparency + completeness) / 3 × 20"
    - path: "docs/harness-spec.md"
      provides: "Harness package format reference"
      contains: "# User Message Template"
    - path: "LIMITATIONS.md"
      provides: "Honest benchmark limitations disclosure"
      contains: "training data leakage"
  key_links:
    - from: "docs/harness-spec.md"
      to: "docs/running.md"
      via: "markdown link"
      pattern: "\\[.*\\].*docs/running\\.md"
    - from: "docs/harness-spec.md"
      to: "harnesses/inventory-optimization/rubric.md"
      via: "markdown link"
      pattern: "\\[.*\\].*rubric\\.md"
    - from: "docs/scoring.md"
      to: "docs/judge-prompt.md"
      via: "markdown link"
      pattern: "\\[.*\\].*judge-prompt\\.md"
---

<objective>
Write three standalone documentation files that establish the scoring methodology, harness authoring spec, and benchmark limitations — all required before README.md can be authored (Plan 02 links to these).

Purpose: These three docs satisfy DOCS-02, DOCS-04, DOCS-05 and provide all link targets README.md will need. DOCS-03 (docs/running.md) is already complete.
Output: docs/scoring.md, docs/harness-spec.md, LIMITATIONS.md
</objective>

<execution_context>
@/Users/obinnaeto/.claude/get-shit-done/workflows/execute-plan.md
@/Users/obinnaeto/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-documentation-and-launch/06-CONTEXT.md
@.planning/phases/06-documentation-and-launch/06-RESEARCH.md

<interfaces>
<!-- All source material for these docs is in the repo. Executor reads these files directly. -->

Key source files (read before writing):
- harnesses/inventory-optimization/harness.yaml — canonical YAML for harness-spec.md field table
- harnesses/inventory-optimization/rubric.md — verbatim score anchors for scoring.md (copy text, do NOT paraphrase)
- harnesses/inventory-optimization/prompt.md — confirms # User Message Template separator pattern
- harnesses/inventory-optimization/schema.ts — schema.ts export pattern for harness-spec.md
- docs/running.md — already complete; harness-spec.md links to it
- docs/judge-prompt.md — scoring.md links to it

Representative scored JSON (embed verbatim in scoring.md):
```json
{
  "run_id": "f39cf220-9c9f-49e5-a820-6b85b37efb12",
  "harness": "inventory-optimization",
  "harness_version": "1.0.0",
  "model": "openai/gpt-4o-mini",
  "composite_score": 67,
  "scores": {
    "actionability": {
      "score": 4,
      "rationale": "Every recommendation specifies a concrete SKU, a definitive action, an exact quantity, and a clear urgency level making it operationally executable; however, SNACK-PRETZEL-1OZ is incorrectly flagged for restock despite having stock well above par, undermining full executability."
    },
    "reasoning_transparency": {
      "score": 3,
      "rationale": "Rationale fields cite current_stock and velocity_7d values but do not show explicit calculations such as days-of-stock-remaining or comparison to par_level thresholds, making reasoning directionally verifiable but not fully reproducible."
    },
    "completeness": {
      "score": 3,
      "rationale": "All three required schema fields are present but data_gaps is an empty array despite obvious missing data points such as expiration dates and margin data, and the summary restates individual product issues rather than synthesizing overall inventory health."
    }
  },
  "schema_valid": true,
  "cost_usd": 0.00047415,
  "latency_ms": 12749,
  "run_date": "2026-03-19T23:57:29.924Z"
}
```

Harness directory layout (for harness-spec.md):
```
harnesses/<harness-name>/
├── harness.yaml          # declarative spec (required)
├── prompt.md             # prompt template with {{placeholder}} injection points
├── schema.ts             # Zod schema exporting a named z.object()
├── rubric.md             # eval rubric with score anchors per dimension
└── data/
    ├── *.csv             # data files; names must match data[].file in harness.yaml
    └── ...
```

harness.yaml field semantics (for harness-spec.md field table):
| Field | Type | Required | Purpose |
|-------|------|----------|---------|
| name | string | yes | Matches directory name; used as harness identifier in CLI (--harness <name>) |
| version | semver string | yes | Bump when rubric or data changes — preserves result comparability |
| description | string | yes | Human-readable summary of the task |
| data[].file | path string | yes | Path relative to harness directory; loaded as raw CSV text |
| data[].inject_as | string | yes | Placeholder name in prompt template — becomes {{inject_as}} in prompt.md |
| prompt.template | path string | yes | Path to prompt.md relative to harness directory |
| output.schema | path string | yes | Path to Zod schema file (schema.ts); loaded via dynamic import at runtime |
| eval.rubric | path string | yes | Path to rubric.md; injected into judge prompt at eval time |
| eval.judge_model | provider/model string | yes | Fixed judge model — must be anthropic/claude-sonnet-4-6 for all reference harnesses |
| eval.judge_temperature | number | yes | Must be 0 for reproducibility |
| providers | string[] | yes | Models this harness has been validated against; advisory (does not restrict which models can run) |

Composite formula:
  composite_score = (actionability + reasoning_transparency + completeness) / 3 × 20
  Range: 0-100. Schema validation failure → composite_score = 0; judge is never called.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write docs/scoring.md</name>
  <files>docs/scoring.md</files>
  <action>
Create docs/scoring.md following this structure (from 06-CONTEXT.md locked decisions):

1. **Why hybrid scoring** — rationale section: business decisions have no ground truth, schema gate catches structural failures before spending judge credits, LLM judge scales without human graders.

2. **Stage 1: JSON schema validation gate** — describe how raw output is validated against the Zod schema via safeParse. Failure writes composite_score of 0 and skips the judge entirely.

3. **Stage 2: LLM judge** — judge model is anthropic/claude-sonnet-4-6 at temperature 0 (fixed for all harnesses). Link to docs/judge-prompt.md for the full prompt text.

4. **Scoring dimensions** — embed verbatim anchor text from harnesses/inventory-optimization/rubric.md for all three dimensions (actionability, reasoning_transparency, completeness). Read the actual rubric file for the exact text — do NOT paraphrase. Note that these same three dimensions apply across all harnesses; harness-specific rubrics apply them to their own domain vocabulary.

5. **Composite formula** — display as a code block:
   ```
   composite_score = (actionability + reasoning_transparency + completeness) / 3 × 20
   ```
   Note: each dimension score is an integer 1-5; result is 0-100.

6. **Representative example** — embed the scored JSON from the interfaces block above verbatim. Verify the math: (4+3+3)/3 × 20 = 66.67 → rounds to 67. Correct.

Do NOT duplicate content from docs/running.md. Link to docs/judge-prompt.md for the full judge prompt rather than reproducing it.
  </action>
  <verify>
    <automated>ls /Users/obinnaeto/Desktop/agentHarness/docs/scoring.md && grep -l "composite_score" /Users/obinnaeto/Desktop/agentHarness/docs/scoring.md && grep -l "judge-prompt" /Users/obinnaeto/Desktop/agentHarness/docs/scoring.md</automated>
  </verify>
  <done>docs/scoring.md exists, contains the composite formula string, links to docs/judge-prompt.md, and embeds verbatim score anchors for all 3 dimensions.</done>
</task>

<task type="auto">
  <name>Task 2: Write docs/harness-spec.md</name>
  <files>docs/harness-spec.md</files>
  <action>
Create docs/harness-spec.md as a reference spec (from 06-CONTEXT.md locked decisions). Follow this structure:

1. **Overview paragraph** — what a harness package is: a self-contained directory that defines a business task, sample data, output schema, and eval rubric for the benchmark runner.

2. **Directory layout** — tree diagram (use the layout from the interfaces block above).

3. **harness.yaml field-by-field table** — use the field table from the interfaces block above. Include all 11 fields with their type, required status, and purpose description. Show the canonical harness.yaml from the interfaces block as a code block (```yaml).

4. **prompt.md conventions** — CRITICAL: explicitly document the `# User Message Template` separator:
   - The exact string `# User Message Template` is required in prompt.md
   - Text above this line becomes the system prompt
   - Text below becomes the user message (after {{placeholder}} injection)
   - Missing the separator causes the loader to throw a runtime error
   - Show the separator pattern from the interfaces block (```markdown example)
   - Document {{placeholder}} resolution: token names must match inject_as values in harness.yaml; unresolved tokens cause a thrown error at load time

5. **schema.ts conventions** — show the named export pattern (```typescript). The named export must be a z.object(). Show the inventory-optimization example from the interfaces block.

6. **rubric.md conventions** — do NOT prescribe dimension names. Link to harnesses/inventory-optimization/rubric.md as the canonical example. Note: RBAB's three reference dimensions (actionability, reasoning_transparency, completeness) are defined there; custom harnesses may define their own.

7. **"Run your harness" link** — point readers to docs/running.md for install and CLI usage. Do NOT duplicate those instructions here.

Do NOT document prompt template injection syntax beyond what's stated above (CONTEXT.md says self-evident from YAML).
  </action>
  <verify>
    <automated>ls /Users/obinnaeto/Desktop/agentHarness/docs/harness-spec.md && grep -l "User Message Template" /Users/obinnaeto/Desktop/agentHarness/docs/harness-spec.md && grep -l "docs/running.md" /Users/obinnaeto/Desktop/agentHarness/docs/harness-spec.md</automated>
  </verify>
  <done>docs/harness-spec.md exists, contains the literal string "# User Message Template" in its prompt.md conventions section, links to docs/running.md, and documents all harness.yaml fields.</done>
</task>

<task type="auto">
  <name>Task 3: Write LIMITATIONS.md</name>
  <files>LIMITATIONS.md</files>
  <action>
Create LIMITATIONS.md in the repo root. From 06-CONTEXT.md locked decisions:

Tone: direct engineering honesty — bullet list, no hedging, no apology.

Structure: title + brief intro sentence, then exactly 4 limitations. Each limitation is a bold heading followed by:
- What the limitation is (1 sentence)
- "What this means for you:" (1 sentence on practical consequence for result interpreters)

The 4 required limitations (no additions, no omissions):

1. **Single-domain scope** — All three harnesses test vending machine operations. What this means for you: scores reflect performance on this specific domain; results may not generalize to other business contexts.

2. **No ground truth** — Business decisions have no objectively correct answer; scoring uses an LLM judge, not verified correct outputs. What this means for you: scores reflect one judge model's assessment of quality; a different judge model may produce different scores.

3. **Single-pass judge** — Each model output is judged once by a single LLM call at temperature 0. What this means for you: scores have no statistical uncertainty bounds; a marginally different output phrasing could shift a score by 1 point.

4. **Training data leakage risk** — The vending machine scenario and evaluation rubric may appear in model training data. What this means for you: scores may reflect memorized patterns rather than live reasoning — we have no way to distinguish this.

Use the exact wording above as the baseline; executor may adjust phrasing for natural flow but MUST NOT change the substance or add a 5th limitation.
  </action>
  <verify>
    <automated>ls /Users/obinnaeto/Desktop/agentHarness/LIMITATIONS.md && grep -c "What this means for you" /Users/obinnaeto/Desktop/agentHarness/LIMITATIONS.md</automated>
  </verify>
  <done>LIMITATIONS.md exists in repo root with exactly 4 limitations, each with a "What this means for you" implication. grep count returns 4.</done>
</task>

</tasks>

<verification>
After all three tasks complete:

```bash
ls /Users/obinnaeto/Desktop/agentHarness/docs/scoring.md \
   /Users/obinnaeto/Desktop/agentHarness/docs/harness-spec.md \
   /Users/obinnaeto/Desktop/agentHarness/LIMITATIONS.md

grep -c "What this means for you" /Users/obinnaeto/Desktop/agentHarness/LIMITATIONS.md
# expected: 4

grep "composite_score" /Users/obinnaeto/Desktop/agentHarness/docs/scoring.md
# expected: at least one match

grep "User Message Template" /Users/obinnaeto/Desktop/agentHarness/docs/harness-spec.md
# expected: at least one match
```
</verification>

<success_criteria>
- docs/scoring.md: exists, contains composite formula, verbatim score anchors for all 3 dimensions, representative scored JSON, link to docs/judge-prompt.md
- docs/harness-spec.md: exists, documents all harness.yaml fields, explicitly calls out # User Message Template separator requirement and {{placeholder}} resolution, links to docs/running.md
- LIMITATIONS.md: exists in repo root, contains exactly 4 limitations each with "What this means for you", no 5th limitation present
- docs/running.md: already complete — no changes made (DOCS-03 satisfied)
</success_criteria>

<output>
After completion, create `.planning/phases/06-documentation-and-launch/06-01-SUMMARY.md`
</output>
