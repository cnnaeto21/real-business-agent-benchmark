---
phase: 02-cli-runner
plan: "02"
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - src/harness.ts
  - src/cost.ts
  - src/output.ts
autonomous: true
requirements:
  - RUN-02
  - RUN-05
  - RUN-06

must_haves:
  truths:
    - "loadHarness('inventory-optimization') returns a rendered prompt with no {{ placeholders and a valid HarnessSpec"
    - "calculateCost returns the correct USD amount for known models and -1 for unknown models"
    - "writeResults creates results/<run-id>/raw/<model-slug>.json and results/<run-id>/meta.json with all required fields"
  artifacts:
    - path: "src/harness.ts"
      provides: "Harness YAML loader, CSV injector, prompt renderer, system/user splitter"
      exports:
        - "loadHarness"
        - "splitPrompt"
    - path: "src/cost.ts"
      provides: "Hardcoded price table and cost calculation"
      exports:
        - "calculateCost"
    - path: "src/output.ts"
      provides: "Writes raw output and meta.json to results/<run-id>/"
      exports:
        - "writeResults"
  key_links:
    - from: "src/harness.ts"
      to: "harnesses/*/harness.yaml"
      via: "js-yaml yaml.load() + readFileSync"
      pattern: "yaml\\.load"
    - from: "src/output.ts"
      to: "src/cost.ts"
      via: "calculateCost import"
      pattern: "calculateCost"

interfaces:
  # Contracts from Plan 02-01 that this plan implements against.
  # Executor should use these directly — no exploration needed.
  from_src_types_ts: |
    export interface HarnessSpec {
      name: string;
      version: string;
      data: Array<{ file: string; inject_as: string }>;
      prompt: { template: string };
      output: { schema: string };
      eval: { rubric: string; judge_model: string; judge_temperature: number };
      providers: string[];
    }

    export interface RunResult {
      rawOutput: unknown;
      inputTokens: number;
      outputTokens: number;
      providerApiVersion: string;
    }

    export interface BenchmarkOptions {
      harness: string;
      model: string;
      temperature: number;
      maxTokens: number;
    }
---

<objective>
Build the three pure-logic modules: harness loader (loads YAML, injects CSVs, splits prompt), cost table (hardcoded price lookup), and output writer (creates directory structure, writes raw JSON and meta manifest).

Purpose: These modules have no provider SDK dependencies — they can be built and unit-tested in parallel with the provider adapters in Plan 02-03. Together with the providers, they give cli.ts everything it needs to wire in Plan 02-04.
Output: src/harness.ts, src/cost.ts, src/output.ts — all importable by src/cli.ts.
</objective>

<execution_context>
@/Users/obinnaeto/.claude/get-shit-done/workflows/execute-plan.md
@/Users/obinnaeto/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-cli-runner/02-RESEARCH.md
@src/types.ts
@harnesses/inventory-optimization/harness.yaml
@harnesses/inventory-optimization/prompt.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build src/harness.ts — YAML loader, CSV injector, prompt renderer, prompt splitter</name>
  <files>src/harness.ts</files>
  <behavior>
    - loadHarness("inventory-optimization") returns an object with `spec` (HarnessSpec) and `renderedPrompt` (string)
    - renderedPrompt has no remaining `{{` or `}}` placeholders after injection
    - renderedPrompt contains the raw CSV file content (first few bytes are the header row)
    - splitPrompt(template) with a template containing "# User Message Template" returns [systemPart, userPart] where neither is empty and systemPart contains "System Prompt"
    - splitPrompt(template) with no separator returns ["", fullTemplate]
    - loadHarness throws with a clear message if harness directory does not exist
    - loadHarness throws with a clear message if the prompt template lacks "# User Message Template" separator
  </behavior>
  <action>
Create `src/harness.ts`. Import from `src/types.ts` for the HarnessSpec interface.

Key implementation notes:
- Use `readFileSync` (not async) — the CLI is sequential; sync reads are simpler and correct here
- Inject CSV content as raw text (do NOT parse CSV into objects or arrays — the prompt expects raw CSV rows)
- Use `{{variable_name}}` replacement matching the `inject_as` field in harness.yaml
- The prompt separator is exactly `# User Message Template` (matches inventory-optimization/prompt.md confirmed)
- After injection, assert that no `{{` remains; throw `Error("Prompt template has unreplaced placeholders: {{ ... }}")` if found — this catches harness authoring errors early
- The `splitPrompt` function must handle missing separator gracefully: return `["", fullTemplate.trim()]`
- Resolve harness directory relative to `process.cwd()` so the CLI works when invoked from the repo root

Export:
```typescript
export function loadHarness(harnessName: string): { spec: HarnessSpec; renderedPrompt: string }
export function splitPrompt(renderedTemplate: string): [string, string]
```
  </action>
  <verify>
    <automated>npx tsx scripts/test-render.ts</automated>
  </verify>
  <done>loadHarness("inventory-optimization") returns a prompt with no placeholders and correct separator. test-render.ts exits 0.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build src/cost.ts and src/output.ts — cost table and results writer</name>
  <files>src/cost.ts, src/output.ts</files>
  <behavior>
    - calculateCost("anthropic/claude-sonnet-4-6", 1_000_000, 1_000_000) returns 18.00 (3.00 + 15.00)
    - calculateCost("openai/gpt-4o", 1_000_000, 1_000_000) returns 12.50 (2.50 + 10.00)
    - calculateCost("google/gemini-1.5-pro", 1_000_000, 1_000_000) returns 6.25 (1.25 + 5.00)
    - calculateCost("unknown/model", 100, 100) returns -1 (sentinel for unknown model)
    - writeResults creates results/<runId>/raw/<modelSlug>.json containing the rawOutput JSON
    - writeResults creates results/<runId>/meta.json containing all 12 required fields
    - writeResults uses mkdir({ recursive: true }) — does not throw if directory already exists
  </behavior>
  <action>
**src/cost.ts:**

Price table verified 2026-03-15 from official pricing pages. Comment the source URLs.

```typescript
// Prices verified 2026-03-15
// Anthropic: https://platform.claude.com/docs/en/about-claude/pricing
// OpenAI: https://openai.com/api/pricing/
// Google: https://ai.google.dev/gemini-api/docs/pricing

const PRICE_TABLE: Record<string, { inputPerMTok: number; outputPerMTok: number }> = {
  "anthropic/claude-sonnet-4-6": { inputPerMTok: 3.00, outputPerMTok: 15.00 },
  "openai/gpt-4o":               { inputPerMTok: 2.50, outputPerMTok: 10.00 },
  "google/gemini-1.5-pro":       { inputPerMTok: 1.25, outputPerMTok: 5.00 },
};

export function calculateCost(model: string, inputTokens: number, outputTokens: number): number {
  const prices = PRICE_TABLE[model];
  if (!prices) return -1; // Unknown model — flag with sentinel value
  return (inputTokens / 1_000_000) * prices.inputPerMTok
       + (outputTokens / 1_000_000) * prices.outputPerMTok;
}
```

**src/output.ts:**

Import `mkdir` and `writeFile` from `fs/promises`, `join` from `path`, and `calculateCost` from `./cost.ts`.

The `writeResults` function signature:
```typescript
export async function writeResults(opts: {
  runId: string;
  modelSlug: string;      // model string with "/" replaced by "--" (e.g. "anthropic--claude-sonnet-4-6")
  model: string;          // original model string (e.g. "anthropic/claude-sonnet-4-6")
  harnessName: string;
  harnessVersion: string;
  rawOutput: unknown;
  inputTokens: number;
  outputTokens: number;
  providerApiVersion: string;
  temperature: number;
  maxTokens: number;
  latencyMs: number;
}): Promise<{ runDir: string }>
```

Directory structure to create:
```
results/
  <runId>/
    raw/
      <modelSlug>.json    <- rawOutput serialized with JSON.stringify(rawOutput, null, 2)
    meta.json             <- manifest with all 12 required fields
```

The meta.json object must contain exactly these 12 keys (matches test-meta.ts):
`run_id`, `harness`, `harness_version`, `model`, `provider_api_version`, `temperature`, `max_tokens`, `input_tokens`, `output_tokens`, `cost_usd`, `latency_ms`, `run_date`

Use `new Date().toISOString()` for `run_date`. Use `calculateCost(opts.model, opts.inputTokens, opts.outputTokens)` for `cost_usd`.

Return `{ runDir }` so cli.ts can log the result path.
  </action>
  <verify>
    <automated>npx tsx scripts/test-meta.ts && npx tsx -e "import { calculateCost } from './src/cost.ts'; const c = calculateCost('anthropic/claude-sonnet-4-6', 1000000, 1000000); console.assert(c === 18, 'cost wrong: ' + c); console.log('cost.ts OK')"</automated>
  </verify>
  <done>calculateCost returns correct values for all three known models and -1 for unknown. test-meta.ts exits 0. writeResults function exported and TypeScript-clean.</done>
</task>

</tasks>

<verification>
After both tasks:
1. `npx tsx scripts/test-render.ts` — exits 0 (loadHarness + splitPrompt work correctly)
2. `npx tsx scripts/test-meta.ts` — exits 0 (all 12 meta fields validated)
3. `npx tsx -e "import { calculateCost } from './src/cost.ts'; console.log(calculateCost('openai/gpt-4o', 1000000, 1000000))"` — prints `12.5`
4. `npx tsx -e "import { loadHarness, splitPrompt } from './src/harness.ts'; const { renderedPrompt } = loadHarness('inventory-optimization'); const [sys, usr] = splitPrompt(renderedPrompt); console.log('system length:', sys.length, 'user length:', usr.length)"` — both lengths > 0
</verification>

<success_criteria>
- src/harness.ts: loadHarness and splitPrompt exported, test-render.ts exits 0
- src/cost.ts: calculateCost returns correct USD values for 3 known models, -1 for unknown
- src/output.ts: writeResults creates correct directory structure with raw JSON and meta.json
- All files compile under tsx with no TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/02-cli-runner/02-02-SUMMARY.md` following the summary template.
</output>
