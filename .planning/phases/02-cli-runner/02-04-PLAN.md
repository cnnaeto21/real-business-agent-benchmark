---
phase: 02-cli-runner
plan: "04"
type: execute
wave: 3
depends_on:
  - "02-02"
  - "02-03"
files_modified:
  - src/cli.ts
  - src/bin.ts
autonomous: false
requirements:
  - RUN-01
  - RUN-02
  - RUN-03
  - RUN-04
  - RUN-05
  - RUN-06

must_haves:
  truths:
    - "Running 'npx tsx src/bin.ts --harness inventory-optimization --model anthropic/claude-sonnet-4-6' completes without error (exit 0)"
    - "A file exists at results/<run-id>/raw/anthropic--claude-sonnet-4-6.json after the run"
    - "A file exists at results/<run-id>/meta.json after the run, containing all 12 required fields"
    - "The rendered prompt is identical across two runs of the same harness (determinism)"
    - "The CLI exits with a non-zero code and a clear error message when --harness or --model is omitted"
  artifacts:
    - path: "src/cli.ts"
      provides: "runBenchmark orchestration: harness load, schema import, provider call, output write"
      exports:
        - "runBenchmark"
    - path: "src/bin.ts"
      provides: "Commander CLI entry point with shebang — wires --harness and --model flags to runBenchmark"
  key_links:
    - from: "src/bin.ts"
      to: "src/cli.ts"
      via: "import { runBenchmark }"
      pattern: "runBenchmark"
    - from: "src/cli.ts"
      to: "src/harness.ts"
      via: "loadHarness + splitPrompt"
      pattern: "loadHarness"
    - from: "src/cli.ts"
      to: "src/providers/index.ts"
      via: "runProvider"
      pattern: "runProvider"
    - from: "src/cli.ts"
      to: "src/output.ts"
      via: "writeResults"
      pattern: "writeResults"
    - from: "src/cli.ts"
      to: "harnesses/*/schema.ts"
      via: "dynamic import() to get Zod schema object + z.toJSONSchema()"
      pattern: "import.*schema"

interfaces:
  # What Plans 02-02 and 02-03 produced — all imports cli.ts needs.
  from_src_harness_ts: |
    export function loadHarness(harnessName: string): { spec: HarnessSpec; renderedPrompt: string }
    export function splitPrompt(renderedTemplate: string): [string, string]

  from_src_providers_index_ts: |
    export async function runProvider(model: string, options: RunOptions): Promise<RunResult>

  from_src_output_ts: |
    export async function writeResults(opts: {
      runId: string; modelSlug: string; model: string;
      harnessName: string; harnessVersion: string; rawOutput: unknown;
      inputTokens: number; outputTokens: number; providerApiVersion: string;
      temperature: number; maxTokens: number; latencyMs: number;
    }): Promise<{ runDir: string }>

  from_src_types_ts: |
    export interface BenchmarkOptions {
      harness: string; model: string; temperature: number; maxTokens: number;
    }
---

<objective>
Wire everything: build src/cli.ts (the orchestration function that sequences harness load → schema import → provider call → output write) and src/bin.ts (the Commander CLI entry that parses flags and calls runBenchmark). Run an end-to-end smoke test against the live Anthropic API, then verify the output files exist and are valid JSON.

Purpose: This plan is the integration layer. All logic already exists in Plans 02-02 and 02-03 — this plan connects it and verifies the full pipeline works end-to-end.
Output: A working `benchmark` CLI. Human verifies the live run produced valid output files.
</objective>

<execution_context>
@/Users/obinnaeto/.claude/get-shit-done/workflows/execute-plan.md
@/Users/obinnaeto/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-cli-runner/02-RESEARCH.md
@src/types.ts
@src/harness.ts
@src/providers/index.ts
@src/output.ts
@harnesses/inventory-optimization/harness.yaml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build src/cli.ts and src/bin.ts — orchestration and CLI entry</name>
  <files>src/cli.ts, src/bin.ts</files>
  <action>
**src/cli.ts** — orchestration:

```typescript
import { randomUUID } from "crypto";
import { join } from "path";
import { z } from "zod";
import { loadHarness, splitPrompt } from "./harness.ts";
import { runProvider } from "./providers/index.ts";
import { writeResults } from "./output.ts";
import type { BenchmarkOptions } from "./types.ts";

async function loadHarnessSchema(harnessName: string): Promise<{ zodSchema: z.ZodType; jsonSchema: object }> {
  const schemaPath = join(process.cwd(), "harnesses", harnessName, "schema.ts");
  const schemaModule = await import(schemaPath);

  // Convention: each schema.ts exports a named Zod schema as the first ZodType export
  const zodSchema = Object.values(schemaModule).find(
    (v): v is z.ZodType => v instanceof z.ZodType
  );
  if (!zodSchema) throw new Error(`No ZodType export found in harnesses/${harnessName}/schema.ts`);

  const jsonSchema = z.toJSONSchema(zodSchema) as object;
  return { zodSchema, jsonSchema };
}

export async function runBenchmark(opts: BenchmarkOptions): Promise<void> {
  const startMs = Date.now();
  const runId = randomUUID();

  console.log(`Starting benchmark run ${runId}`);
  console.log(`  Harness: ${opts.harness}`);
  console.log(`  Model:   ${opts.model}`);

  // 1. Load harness spec and render prompt
  const { spec, renderedPrompt } = loadHarness(opts.harness);

  // 2. Split rendered prompt into system and user parts
  const [systemPrompt, userMessage] = splitPrompt(renderedPrompt);

  // 3. Load Zod schema (provides both zodSchema for OpenAI and jsonSchema for Anthropic/Google)
  const { zodSchema, jsonSchema } = await loadHarnessSchema(opts.harness);

  // 4. Call provider (routes by "provider/" prefix)
  const result = await runProvider(opts.model, {
    modelId: opts.model.split("/").slice(1).join("/"),
    systemPrompt,
    userMessage,
    jsonSchema,
    zodSchema,
    temperature: opts.temperature,
    maxTokens: opts.maxTokens,
  });

  const latencyMs = Date.now() - startMs;

  // 5. Sanitize model string for use as filename — "/" is illegal in filenames
  const modelSlug = opts.model.replace(/\//g, "--");

  // 6. Write raw output and meta manifest
  const { runDir } = await writeResults({
    runId,
    modelSlug,
    model: opts.model,
    harnessName: spec.name,
    harnessVersion: spec.version,
    rawOutput: result.rawOutput,
    inputTokens: result.inputTokens,
    outputTokens: result.outputTokens,
    providerApiVersion: result.providerApiVersion,
    temperature: opts.temperature,
    maxTokens: opts.maxTokens,
    latencyMs,
  });

  console.log(`\nRun complete: ${runDir}`);
  console.log(`  Tokens:  ${result.inputTokens} in / ${result.outputTokens} out`);
  console.log(`  Latency: ${latencyMs}ms`);
}
```

**src/bin.ts** — Commander entry point:

```typescript
#!/usr/bin/env node
import { Command } from "commander";
import { runBenchmark } from "./cli.ts";

const program = new Command();

program
  .name("benchmark")
  .description("Run a benchmark harness against an LLM provider")
  .requiredOption("--harness <name>", "Harness name (e.g. inventory-optimization)")
  .requiredOption("--model <provider/model-id>", "Model string (e.g. anthropic/claude-sonnet-4-6)")
  .option("--temperature <number>", "Sampling temperature (default: 0)", "0")
  .option("--max-tokens <number>", "Max output tokens (default: 4096)", "4096")
  .action(async (options) => {
    try {
      await runBenchmark({
        harness: options.harness,
        model: options.model,
        temperature: parseFloat(options.temperature),
        maxTokens: parseInt(options.maxTokens, 10),
      });
    } catch (err) {
      console.error("Benchmark failed:", err instanceof Error ? err.message : err);
      process.exit(1);
    }
  });

program.parse();
```
  </action>
  <verify>
    <automated>npx tsx -e "import { runBenchmark } from './src/cli.ts'; console.log(typeof runBenchmark === 'function' ? 'cli.ts OK' : 'FAIL')" && npx tsx src/bin.ts --help</automated>
  </verify>
  <done>src/cli.ts and src/bin.ts exist and compile. `npx tsx src/bin.ts --help` prints usage with --harness and --model flags listed.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: End-to-end smoke test — verify live Anthropic run produces correct output files</name>
  <what-built>
Complete benchmark CLI pipeline:
- src/bin.ts: Commander CLI entry with --harness and --model flags
- src/cli.ts: Orchestration — loads harness, imports schema, calls provider, writes output files
- src/harness.ts: YAML loader + CSV injector + prompt renderer (from Plan 02-02)
- src/providers/: Dispatcher + 3 adapters (from Plan 02-03)
- src/output.ts: results directory writer (from Plan 02-02)

Before this checkpoint, run the live smoke test:
```bash
npx tsx src/bin.ts --harness inventory-optimization --model anthropic/claude-sonnet-4-6
```
(Requires ANTHROPIC_API_KEY in environment.)
  </what-built>
  <how-to-verify>
1. Set your API key: `export ANTHROPIC_API_KEY=sk-ant-...`
2. Run: `npx tsx src/bin.ts --harness inventory-optimization --model anthropic/claude-sonnet-4-6`
3. Confirm the command exits 0 and prints a "Run complete: results/..." line
4. Find the run ID in the output, then verify the files exist:
   ```bash
   ls results/<run-id>/raw/
   cat results/<run-id>/raw/anthropic--claude-sonnet-4-6.json | head -20
   cat results/<run-id>/meta.json
   ```
5. Confirm raw output JSON has top-level keys: `summary`, `recommendations`, `data_gaps`
6. Confirm meta.json has all of: `run_id`, `harness`, `harness_version`, `model`, `provider_api_version`, `temperature`, `max_tokens`, `input_tokens`, `output_tokens`, `cost_usd`, `latency_ms`, `run_date`
7. Run a second time and confirm a new `results/<run-id>/` directory is created (different UUID)
  </how-to-verify>
  <resume-signal>Type "approved" if the run completed and files look correct, or describe any errors encountered</resume-signal>
  <action>Run: npx tsx src/bin.ts --harness inventory-optimization --model anthropic/claude-sonnet-4-6 (requires ANTHROPIC_API_KEY). Inspect results/ directory for raw output and meta.json files.</action>
  <verify>
    <automated>ls results/*/raw/anthropic--claude-sonnet-4-6.json 2>/dev/null | head -1 || echo 'No output file yet — run smoke test first'</automated>
  </verify>
  <done>Run completes with exit 0, results/&lt;run-id&gt;/raw/anthropic--claude-sonnet-4-6.json and results/&lt;run-id&gt;/meta.json both exist, human confirms JSON content matches schema fields</done>
</task>

</tasks>

<verification>
Full suite after human approval:
1. `npx tsx scripts/test-render.ts` — exits 0
2. `npx tsx scripts/test-routing.ts` — exits 0
3. `npx tsx scripts/test-meta.ts` — exits 0
4. `npx tsx src/bin.ts --help` — shows usage
5. `ls results/` — shows at least one run directory from the smoke test
6. Raw output JSON validates against the inventory-optimization schema fields
</verification>

<success_criteria>
- `npx tsx src/bin.ts --harness inventory-optimization --model anthropic/claude-sonnet-4-6` completes with exit 0
- `results/<run-id>/raw/anthropic--claude-sonnet-4-6.json` exists and contains valid JSON with summary, recommendations, data_gaps
- `results/<run-id>/meta.json` exists and contains all 12 required fields
- All three unit test scripts pass
- Phase goal achieved: a working benchmark CLI that loads a harness, renders a prompt with real data, calls one LLM provider, and writes raw output to disk
</success_criteria>

<output>
After completion, create `.planning/phases/02-cli-runner/02-04-SUMMARY.md` following the summary template.
</output>
