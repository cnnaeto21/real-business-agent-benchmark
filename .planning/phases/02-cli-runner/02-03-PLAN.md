---
phase: 02-cli-runner
plan: "03"
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - src/providers/index.ts
  - src/providers/anthropic.ts
  - src/providers/openai.ts
  - src/providers/google.ts
autonomous: true
requirements:
  - RUN-03
  - RUN-04

must_haves:
  truths:
    - "src/providers/index.ts routes anthropic/ prefix to anthropic adapter, openai/ to openai, google/ to google, throws for unknown prefix"
    - "Anthropic adapter calls the Anthropic SDK with tool use + tool_choice: {type: 'tool', name: 'structured_output'} — NOT output_config.format (per RUN-04 requirement)"
    - "OpenAI adapter uses client.beta.chat.completions.parse with zodResponseFormat — NOT json_schema mode"
    - "Google adapter uses @google/genai (NOT @google/generative-ai legacy) with responseMimeType: 'application/json' and responseSchema"
    - "All three adapters return a RunResult with rawOutput, inputTokens, outputTokens, providerApiVersion"
    - "Model slug sanitization: '/' replaced with '--' before use in filenames (handled in cli.ts, not adapters)"
  artifacts:
    - path: "src/providers/index.ts"
      provides: "Provider dispatcher: routes by model prefix, exports runProvider"
      exports:
        - "runProvider"
    - path: "src/providers/anthropic.ts"
      provides: "Anthropic adapter using tool use structured output"
      exports:
        - "runAnthropic"
    - path: "src/providers/openai.ts"
      provides: "OpenAI adapter using zodResponseFormat"
      exports:
        - "runOpenAI"
    - path: "src/providers/google.ts"
      provides: "Google adapter using @google/genai responseMimeType"
      exports:
        - "runGoogle"
  key_links:
    - from: "src/providers/index.ts"
      to: "src/providers/anthropic.ts, src/providers/openai.ts, src/providers/google.ts"
      via: "dynamic import() on provider prefix match"
      pattern: "await import"
    - from: "src/providers/anthropic.ts"
      to: "@anthropic-ai/sdk"
      via: "Anthropic client.messages.create with tool use"
      pattern: "tool_choice.*type.*tool"
    - from: "src/providers/openai.ts"
      to: "openai"
      via: "client.beta.chat.completions.parse + zodResponseFormat"
      pattern: "zodResponseFormat"
    - from: "src/providers/google.ts"
      to: "@google/genai"
      via: "GoogleGenAI ai.models.generateContent with responseMimeType"
      pattern: "responseMimeType"

interfaces:
  # Contracts from Plan 02-01 that this plan implements against.
  from_src_types_ts: |
    export interface RunOptions {
      modelId: string;           // provider prefix already stripped
      systemPrompt: string;
      userMessage: string;
      jsonSchema: object;        // z.toJSONSchema() output — for Anthropic + Google
      zodSchema: unknown;        // z.ZodType — for OpenAI zodResponseFormat
      temperature: number;
      maxTokens: number;
    }

    export interface RunResult {
      rawOutput: unknown;
      inputTokens: number;
      outputTokens: number;
      providerApiVersion: string;
    }
---

<objective>
Build all four provider files: the dispatcher that routes by model prefix, and the three adapters (Anthropic tool use, OpenAI zodResponseFormat, Google responseMimeType). Each adapter implements the same RunOptions → RunResult contract.

Purpose: These files can be built in parallel with Plan 02-02 (no shared file ownership). They depend only on the type contracts from Plan 02-01.
Output: src/providers/index.ts, src/providers/anthropic.ts, src/providers/openai.ts, src/providers/google.ts.
</objective>

<execution_context>
@/Users/obinnaeto/.claude/get-shit-done/workflows/execute-plan.md
@/Users/obinnaeto/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-cli-runner/02-RESEARCH.md
@src/types.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build src/providers/index.ts — provider dispatcher</name>
  <files>src/providers/index.ts</files>
  <action>
Create `src/providers/` directory and `src/providers/index.ts`.

The dispatcher function:
- Accepts the full model string including prefix (e.g. "anthropic/claude-sonnet-4-6")
- Routes by prefix to the correct adapter via dynamic `import()`
- Passes `modelId` to the adapter as the prefix-stripped model name (e.g. "claude-sonnet-4-6")
- Throws `Error("Unknown provider prefix in model: ${model}")` for unrecognized prefixes

```typescript
import type { RunOptions, RunResult } from "../types.ts";

export type { RunOptions, RunResult };

export async function runProvider(model: string, options: RunOptions): Promise<RunResult> {
  if (model.startsWith("anthropic/")) {
    const { runAnthropic } = await import("./anthropic.js");
    return runAnthropic(model.slice("anthropic/".length), options);
  } else if (model.startsWith("openai/")) {
    const { runOpenAI } = await import("./openai.js");
    return runOpenAI(model.slice("openai/".length), options);
  } else if (model.startsWith("google/")) {
    const { runGoogle } = await import("./google.js");
    return runGoogle(model.slice("google/".length), options);
  }
  throw new Error(`Unknown provider prefix in model: ${model}`);
}
```

Note: Dynamic imports use `.js` extension per ESM convention (tsx resolves `.js` to `.ts` at runtime).
  </action>
  <verify>
    <automated>npx tsx scripts/test-routing.ts</automated>
  </verify>
  <done>test-routing.ts exits 0. All three prefixes route correctly, unknown prefix throws.</done>
</task>

<task type="auto">
  <name>Task 2: Build Anthropic, OpenAI, and Google provider adapters</name>
  <files>src/providers/anthropic.ts, src/providers/openai.ts, src/providers/google.ts</files>
  <action>
Create all three adapter files. Each implements `RunOptions → Promise<RunResult>`.

**CRITICAL: Anthropic adapter must use tool use (per RUN-04 requirement), NOT output_config.format / zodOutputFormat. The requirement was authored before native Structured Outputs GA; honor the requirement as written.**

**src/providers/anthropic.ts:**
```typescript
// RUN-04: Uses Anthropic tool use structured output (tool_choice forced).
// Do NOT use output_config.format / zodOutputFormat — requirement explicitly specifies tool use.
import Anthropic from "@anthropic-ai/sdk";
import type { RunOptions, RunResult } from "../types.ts";

const ANTHROPIC_API_VERSION = "2023-06-01"; // Stable version; all SDK calls use this

export async function runAnthropic(modelId: string, opts: RunOptions): Promise<RunResult> {
  const client = new Anthropic({ apiKey: process.env.ANTHROPIC_API_KEY });

  const response = await client.messages.create({
    model: modelId,
    max_tokens: opts.maxTokens,
    temperature: opts.temperature,
    system: opts.systemPrompt,
    tools: [{
      name: "structured_output",
      description: "Return your analysis in the required structured format",
      input_schema: opts.jsonSchema as Anthropic.Tool["input_schema"],
    }],
    tool_choice: { type: "tool", name: "structured_output" },
    messages: [{ role: "user", content: opts.userMessage }],
  });

  const toolBlock = response.content.find((b) => b.type === "tool_use");
  if (!toolBlock || toolBlock.type !== "tool_use") {
    throw new Error("Anthropic response did not contain expected tool_use block");
  }

  return {
    rawOutput: toolBlock.input,
    // Note: Anthropic usage.input_tokens already includes tool-use overhead tokens — no adjustment needed
    inputTokens: response.usage.input_tokens,
    outputTokens: response.usage.output_tokens,
    providerApiVersion: ANTHROPIC_API_VERSION,
  };
}
```

**src/providers/openai.ts:**
```typescript
// RUN-04: Uses OpenAI zodResponseFormat + beta.chat.completions.parse.
// Do NOT use response_format: {type: "json_object"} (JSON mode) — does not enforce schema.
import OpenAI from "openai";
import { zodResponseFormat } from "openai/helpers/zod";
import { z } from "zod";
import type { RunOptions, RunResult } from "../types.ts";

export async function runOpenAI(modelId: string, opts: RunOptions): Promise<RunResult> {
  const client = new OpenAI({ apiKey: process.env.OPENAI_API_KEY });

  // zodResponseFormat requires the Zod schema object, NOT the JSON Schema from z.toJSONSchema()
  const completion = await client.beta.chat.completions.parse({
    model: modelId,
    max_tokens: opts.maxTokens,
    temperature: opts.temperature,
    messages: [
      { role: "system", content: opts.systemPrompt },
      { role: "user", content: opts.userMessage },
    ],
    response_format: zodResponseFormat(opts.zodSchema as z.ZodType, "structured_output"),
  });

  const parsed = completion.choices[0].message.parsed;
  if (!parsed) throw new Error("OpenAI response parsing failed — no parsed output");

  return {
    rawOutput: parsed,
    inputTokens: completion.usage?.prompt_tokens ?? 0,
    outputTokens: completion.usage?.completion_tokens ?? 0,
    providerApiVersion: "openai-chat-completions-v1",
  };
}
```

**src/providers/google.ts:**
```typescript
// RUN-04: Uses @google/genai (NOT @google/generative-ai — that package is EOL August 31, 2025).
// GOOGLE_API_KEY must be a Gemini Developer API key from aistudio.google.com — NOT a GCP service account key.
import { GoogleGenAI } from "@google/genai";
import type { RunOptions, RunResult } from "../types.ts";

export async function runGoogle(modelId: string, opts: RunOptions): Promise<RunResult> {
  const ai = new GoogleGenAI({ apiKey: process.env.GOOGLE_API_KEY });

  const response = await ai.models.generateContent({
    model: modelId,
    contents: opts.userMessage,
    config: {
      systemInstruction: opts.systemPrompt,
      responseMimeType: "application/json",
      responseSchema: opts.jsonSchema,  // JSON Schema from z.toJSONSchema() — Google accepts this directly
      temperature: opts.temperature,
      maxOutputTokens: opts.maxTokens,
    },
  });

  const rawText = response.text;
  if (!rawText) throw new Error("Google response contained no text output");

  return {
    rawOutput: JSON.parse(rawText),
    inputTokens: response.usageMetadata?.promptTokenCount ?? 0,
    outputTokens: response.usageMetadata?.candidatesTokenCount ?? 0,
    providerApiVersion: "gemini-api-v1beta",
  };
}
```

After writing all three files, verify TypeScript types compile under tsx. If the Google adapter has a field name mismatch (responseSchema vs responseJsonSchema — see research open question #3), check the installed `@google/genai` TypeScript types:
```bash
grep -r "responseSchema\|responseJsonSchema" node_modules/@google/genai/build/src/types.d.ts 2>/dev/null | head -5
```
Use whichever field name the TypeScript types export. Update the Google adapter accordingly.
  </action>
  <verify>
    <automated>npx tsx -e "import { runProvider } from './src/providers/index.ts'; console.log(typeof runProvider === 'function' ? 'providers OK' : 'FAIL')" && npx tsx scripts/test-routing.ts</automated>
  </verify>
  <done>All four provider files exist and compile under tsx. runProvider is exported as a function. test-routing.ts exits 0. Google adapter uses correct field name per installed TypeScript types.</done>
</task>

</tasks>

<verification>
After both tasks:
1. `npx tsx scripts/test-routing.ts` — exits 0
2. `npx tsx -e "import { runProvider } from './src/providers/index.ts'; console.log('dispatcher OK')"` — no import errors
3. `npx tsx -e "import { runAnthropic } from './src/providers/anthropic.ts'; console.log('anthropic OK')"` — no import errors
4. `npx tsx -e "import { runOpenAI } from './src/providers/openai.ts'; console.log('openai OK')"` — no import errors
5. `npx tsx -e "import { runGoogle } from './src/providers/google.ts'; console.log('google OK')"` — no import errors
</verification>

<success_criteria>
- src/providers/index.ts: runProvider exported, routes all three prefixes correctly, throws on unknown
- src/providers/anthropic.ts: uses tool use with tool_choice forced (NOT output_config.format)
- src/providers/openai.ts: uses zodResponseFormat with beta.chat.completions.parse
- src/providers/google.ts: uses @google/genai with responseMimeType (NOT @google/generative-ai)
- All four files compile with no TypeScript errors under tsx
</success_criteria>

<output>
After completion, create `.planning/phases/02-cli-runner/02-03-SUMMARY.md` following the summary template.
</output>
