---
phase: 02-cli-runner
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - .gitignore
  - src/types.ts
  - scripts/test-render.ts
  - scripts/test-routing.ts
  - scripts/test-meta.ts
autonomous: true
requirements:
  - RUN-01
  - RUN-02
  - RUN-03
  - RUN-06

must_haves:
  truths:
    - "npm install completes without error and adds commander, @anthropic-ai/sdk, openai, @google/genai, js-yaml to node_modules"
    - "results/ is listed in .gitignore so dev runs are never accidentally committed"
    - "src/types.ts exports RunOptions, RunResult, HarnessSpec, BenchmarkOptions — the contracts all other modules implement against"
    - "scripts/test-render.ts runs to completion (exit 0) and asserts no {{ placeholders remain in a rendered prompt"
    - "scripts/test-routing.ts runs to completion (exit 0) and asserts prefix dispatch selects the correct provider string"
    - "scripts/test-meta.ts runs to completion (exit 0) and asserts all required meta.json fields are present"
  artifacts:
    - path: "src/types.ts"
      provides: "Shared TypeScript contracts for all Phase 2 modules"
      exports:
        - "HarnessSpec"
        - "RunOptions"
        - "RunResult"
        - "BenchmarkOptions"
    - path: "scripts/test-render.ts"
      provides: "RUN-02 unit test — prompt renderer"
    - path: "scripts/test-routing.ts"
      provides: "RUN-03 unit test — provider routing"
    - path: "scripts/test-meta.ts"
      provides: "RUN-06 unit test — meta.json field presence"
  key_links:
    - from: "src/types.ts"
      to: "src/harness.ts, src/providers/index.ts, src/output.ts, src/cli.ts"
      via: "TypeScript imports"
      pattern: "import.*from.*types"
---

<objective>
Bootstrap the CLI runner: install dependencies, protect the results directory from git, define shared TypeScript contracts, and create the three unit-test scripts that will verify the core runner behaviors without making live API calls.

Purpose: Plans 02 and 03 (harness loader, providers) can both reference `src/types.ts` contracts directly without exploring the codebase. Test scripts exist before any implementation so the Nyquist rule is satisfied.
Output: Updated package.json with new deps, updated .gitignore, src/types.ts, scripts/test-render.ts, scripts/test-routing.ts, scripts/test-meta.ts.
</objective>

<execution_context>
@/Users/obinnaeto/.claude/get-shit-done/workflows/execute-plan.md
@/Users/obinnaeto/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-cli-runner/02-RESEARCH.md
@.planning/phases/02-cli-runner/02-VALIDATION.md
@harnesses/inventory-optimization/harness.yaml
@harnesses/inventory-optimization/schema.ts
@harnesses/inventory-optimization/prompt.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install dependencies, update .gitignore, and add package.json scripts</name>
  <files>package.json, .gitignore</files>
  <action>
Install the five new production dependencies:

```bash
npm install commander @anthropic-ai/sdk openai @google/genai js-yaml
npm install --save-dev @types/js-yaml
```

Then update package.json:
- Add `"bin": { "benchmark": "./src/bin.ts" }` field at the top level
- Add `"benchmark": "tsx src/bin.ts"` to the `"scripts"` section alongside the existing `"validate"` script

Then update .gitignore to add `results/` on its own line. The existing .gitignore has `node_modules/`, `dist/`, and `*.js.map`. Add `results/` below those.

Do NOT modify any other package.json fields. The project uses `"type": "module"` and `"private": true` — leave them as-is.
  </action>
  <verify>
    <automated>node -e "const p = JSON.parse(require('fs').readFileSync('package.json','utf-8')); console.assert(p.dependencies.commander, 'commander missing'); console.assert(p.dependencies['@anthropic-ai/sdk'], 'sdk missing'); console.assert(p.bin && p.bin.benchmark, 'bin missing'); console.log('package.json OK')" && grep -q "results/" .gitignore && echo ".gitignore OK"</automated>
  </verify>
  <done>All six packages in node_modules, bin field present in package.json, results/ in .gitignore</done>
</task>

<task type="auto">
  <name>Task 2: Define shared TypeScript contracts in src/types.ts</name>
  <files>src/types.ts</files>
  <action>
Create `src/types.ts` with the following exported interfaces. These are the contracts every other Phase 2 module imports. No implementation logic — types only.

```typescript
// src/types.ts
// Shared contracts for the benchmark CLI runner (Phase 2).
// All provider adapters, harness loader, and output writer import from here.

export interface HarnessSpec {
  name: string;
  version: string;
  data: Array<{ file: string; inject_as: string }>;
  prompt: { template: string };
  output: { schema: string };
  eval: { rubric: string; judge_model: string; judge_temperature: number };
  providers: string[];
}

/**
 * Options passed to every provider adapter.
 * jsonSchema: z.toJSONSchema() output — used by Anthropic (input_schema) and Google (responseSchema).
 * zodSchema: raw Zod object — required by OpenAI zodResponseFormat (cannot accept plain JSON Schema).
 */
export interface RunOptions {
  modelId: string;           // e.g. "claude-sonnet-4-6" (prefix stripped)
  systemPrompt: string;
  userMessage: string;
  jsonSchema: object;        // z.toJSONSchema() output
  zodSchema: unknown;        // z.ZodType — typed as unknown here to avoid zod import in types.ts
  temperature: number;
  maxTokens: number;
}

/**
 * Returned by every provider adapter.
 */
export interface RunResult {
  rawOutput: unknown;          // Parsed structured output object
  inputTokens: number;
  outputTokens: number;
  providerApiVersion: string;  // e.g. "2023-06-01" for Anthropic
}

/**
 * CLI entry options parsed from Commander.
 */
export interface BenchmarkOptions {
  harness: string;    // e.g. "inventory-optimization"
  model: string;      // e.g. "anthropic/claude-sonnet-4-6"
  temperature: number;
  maxTokens: number;
}
```

Create the `src/` directory if it does not exist.
  </action>
  <verify>
    <automated>npx tsx -e "import type { HarnessSpec, RunOptions, RunResult, BenchmarkOptions } from './src/types.ts'; console.log('types.ts compiles OK')"</automated>
  </verify>
  <done>src/types.ts exists, all four interfaces exported, file compiles with no TypeScript errors under tsx</done>
</task>

<task type="auto">
  <name>Task 3: Create Wave 0 test scripts (test-render.ts, test-routing.ts, test-meta.ts)</name>
  <files>scripts/test-render.ts, scripts/test-routing.ts, scripts/test-meta.ts</files>
  <action>
Create three test scripts that use only Node `assert` (no test framework). Each must exit 0 on success and exit non-zero on failure.

**scripts/test-render.ts** — verifies RUN-02 (prompt rendering, no placeholder leakage):
```typescript
import { readFileSync } from "fs";
import { join } from "path";
import assert from "assert";
import yaml from "js-yaml";
import type { HarnessSpec } from "../src/types.ts";

// Load inventory-optimization harness and render the prompt template
const harnessDir = join(process.cwd(), "harnesses", "inventory-optimization");
const spec = yaml.load(readFileSync(join(harnessDir, "harness.yaml"), "utf-8")) as HarnessSpec;

let template = readFileSync(join(harnessDir, spec.prompt.template), "utf-8");

for (const dataFile of spec.data) {
  const csv = readFileSync(join(harnessDir, dataFile.file), "utf-8");
  template = template.replace(`{{${dataFile.inject_as}}}`, csv);
}

// Assert no unreplaced placeholders remain
assert.ok(!template.includes("{{"), `Unreplaced placeholder found in rendered prompt`);
assert.ok(!template.includes("}}"), `Unreplaced placeholder found in rendered prompt`);

// Assert separator exists (required by splitPrompt in cli.ts)
assert.ok(
  template.includes("# User Message Template"),
  "Prompt template missing '# User Message Template' separator"
);

console.log("test-render: PASS — prompt renders without placeholders, separator present");
```

**scripts/test-routing.ts** — verifies RUN-03 (provider prefix dispatch logic):
```typescript
import assert from "assert";

// Simulate the routing logic from src/providers/index.ts
function getProvider(model: string): string {
  if (model.startsWith("anthropic/")) return "anthropic";
  if (model.startsWith("openai/")) return "openai";
  if (model.startsWith("google/")) return "google";
  throw new Error(`Unknown provider prefix in model: ${model}`);
}

assert.strictEqual(getProvider("anthropic/claude-sonnet-4-6"), "anthropic");
assert.strictEqual(getProvider("openai/gpt-4o"), "openai");
assert.strictEqual(getProvider("google/gemini-1.5-pro"), "google");

// Assert unknown prefix throws
assert.throws(() => getProvider("unknown/model"), /Unknown provider prefix/);

console.log("test-routing: PASS — all three providers route correctly, unknown prefix throws");
```

**scripts/test-meta.ts** — verifies RUN-06 (meta.json required field presence):
```typescript
import assert from "assert";

// Required fields for results/<run-id>/meta.json per RUN-06
const REQUIRED_META_FIELDS = [
  "run_id",
  "harness",
  "harness_version",
  "model",
  "provider_api_version",
  "temperature",
  "max_tokens",
  "input_tokens",
  "output_tokens",
  "cost_usd",
  "latency_ms",
  "run_date",
];

// Simulate a meta object as output.ts would produce it
const sampleMeta = {
  run_id: "test-uuid-1234",
  harness: "inventory-optimization",
  harness_version: "1.0.0",
  model: "anthropic/claude-sonnet-4-6",
  provider_api_version: "2023-06-01",
  temperature: 0,
  max_tokens: 4096,
  input_tokens: 1000,
  output_tokens: 500,
  cost_usd: 0.01,
  latency_ms: 2500,
  run_date: new Date().toISOString(),
};

for (const field of REQUIRED_META_FIELDS) {
  assert.ok(field in sampleMeta, `Required meta field missing: ${field}`);
}

console.log("test-meta: PASS — all required meta.json fields present");
```
  </action>
  <verify>
    <automated>npx tsx scripts/test-render.ts && npx tsx scripts/test-routing.ts && npx tsx scripts/test-meta.ts</automated>
  </verify>
  <done>All three scripts exit 0. test-render confirms no {{ placeholders and separator present. test-routing confirms all three prefixes dispatch correctly. test-meta confirms all 12 required fields are validated.</done>
</task>

</tasks>

<verification>
After all tasks:
1. `npm install` already ran as part of Task 1 — node_modules contains commander, @anthropic-ai/sdk, openai, @google/genai, js-yaml
2. `cat .gitignore` shows `results/` on a line
3. `cat package.json | grep benchmark` shows both the bin field and script entry
4. `npx tsx scripts/test-render.ts && npx tsx scripts/test-routing.ts && npx tsx scripts/test-meta.ts` — all three exit 0
</verification>

<success_criteria>
- npm install succeeds with all 6 new packages (5 prod + @types/js-yaml dev)
- .gitignore includes `results/`
- src/types.ts exports HarnessSpec, RunOptions, RunResult, BenchmarkOptions with no TypeScript errors
- All three test scripts pass (exit 0) using only the existing harness files — no live API calls required
</success_criteria>

<output>
After completion, create `.planning/phases/02-cli-runner/02-01-SUMMARY.md` following the summary template.
</output>
